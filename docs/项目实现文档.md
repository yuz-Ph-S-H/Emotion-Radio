# 「共感电台」—— 文字驱动的实时情绪音乐与视觉生成系统

## 完整项目实现方案（一日速成版）

---

## 一、项目概述

### 1.1 项目简介

「共感电台」是一个基于文字情绪分析的实时音乐与视觉生成系统。用户输入任意中文文字，系统实时分析情感倾向与意象关键词，同步生成匹配的音乐旋律与粒子视觉特效。

### 1.2 核心体验

输入"今天下了一场大雨，我一个人走在路上" → 背景渐变为深蓝色，屏幕上下起粒子雨，低沉的钢琴旋律响起。继续输入"突然看到一道彩虹" → 画面明亮，彩色光弧浮现，音乐转为温暖弦乐。

### 1.3 两套NLP方案

| 方案 | 技术 | 费用 | 效果 | 适用场景 |
|------|------|:---:|:---:|----------|
| A. 本地方案（默认） | jieba + SnowNLP + 自建词典 | 免费 | ★★★★☆ | 无网络/零成本 |
| B. API增强方案（可选） | 大语言模型API | 免费额度或极低成本 | ★★★★★ | 追求更精准的语义理解 |

两套方案**代码已全部提供**，通过配置文件一键切换。

---

## 二、今日完成计划

> 以下为一个人借助 Claude 在一天内完成全部功能的时间规划。

| 时间段 | 任务 | 操作方式 |
|--------|------|----------|
| 第1小时 | 环境搭建 + 目录创建 + 依赖安装 | 复制本文档中的命令直接执行 |
| 第2小时 | 后端NLP模块（含词典） | 复制 `app.py`、`nlp/` 目录下所有文件 |
| 第3~4小时 | 前端页面 + CSS + WebSocket通信 | 复制 `templates/`、`static/css/`、`static/js/socket.js`、`main.js` |
| 第5~6小时 | 音乐引擎 | 复制 `music-engine.js` 并调试 |
| 第7~8小时 | 视觉引擎 + 粒子系统 | 复制 `visual-engine.js`、`particle.js` 并调试 |
| 第9小时 | 时间线 + 全模块联调 | 复制 `timeline.js`、`utils.js`，整体联调 |
| 第10小时 | 效果打磨 + 演示排练 | 调整参数、准备演示文案 |

**关键原则：** 所有代码均可直接复制粘贴运行，先跑通再调优。

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────┐
│                    浏览器（前端）                          │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │  文字输入UI   │  │ 音乐生成引擎  │  │  视觉特效引擎   │  │
│  │  + 时间线     │  │  (Tone.js)   │  │  (Canvas 2D)   │  │
│  └──────┬──────┘  └──────▲───────┘  └───────▲────────┘  │
│         │         ┌──────┴──────────────────┴──────┐     │
│         │         │      情绪参数分发器              │     │
│         │         └──────▲────────────────────────┘     │
│         │                │                               │
└─────────┼────────────────┼───────────────────────────────┘
          │  WebSocket     │  WebSocket
          ▼                │
┌─────────┴────────────────┴───────────────────────────────┐
│                  Python 后端（Flask）                      │
│                                                          │
│  ┌──────────────────────────────────────────────────┐    │
│  │ 方案A: jieba分词 + SnowNLP + 词典匹配             │    │
│  │ 方案B: 大语言模型API（通义千问/DeepSeek等）        │    │
│  └──────────────────────────────────────────────────┘    │
│                          ↓                               │
│                  ┌───────────────┐                       │
│                  │  情绪参数包整合 │                       │
│                  └───────────────┘                       │
└──────────────────────────────────────────────────────────┘
```

---

## 四、环境搭建（10分钟）

```bash
# 1. 创建项目目录
mkdir emotion-radio && cd emotion-radio

# 2. 创建Python虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. 安装全部依赖
pip install flask flask-socketio eventlet jieba snownlp requests

# 4. 创建目录结构
mkdir -p nlp templates static/js static/css data

# 5. 创建nlp包初始化文件
touch nlp/__init__.py
```

---

## 五、项目目录结构

```
emotion-radio/
├── app.py                     # Flask主应用入口
├── config.py                  # 全局配置（NLP方案切换）
├── requirements.txt           # Python依赖
│
├── nlp/
│   ├── __init__.py
│   ├── analyzer.py            # 文本分析主逻辑
│   ├── imagery_dict.py        # 意象-情绪映射词典（300+词条）
│   ├── emotion_model.py       # 情绪参数计算
│   └── api_analyzer.py        # API增强方案（可选）
│
├── templates/
│   └── index.html             # 主页面
│
├── static/
│   ├── css/
│   │   └── style.css          # 界面样式
│   └── js/
│       ├── main.js            # 主控逻辑
│       ├── socket.js          # WebSocket通信
│       ├── music-engine.js    # 音乐生成引擎
│       ├── visual-engine.js   # 视觉特效引擎
│       ├── particle.js        # 粒子系统
│       ├── effects.js         # 高级视觉特效（极光/水波/烟雾等）
│       ├── timeline.js        # 情绪时间线
│       └── utils.js           # 工具函数
│
└── data/
    └── custom_dict.txt        # jieba自定义词典（可选）
```

---

## 六、配置文件 `config.py`

```python
"""
全局配置 —— 通过修改此文件切换NLP方案
"""

# ========== NLP方案选择 ==========
# "local"  → 方案A：jieba + SnowNLP + 词典（免费离线）
# "api"    → 方案B：大语言模型API（更精准）
NLP_MODE = "local"

# ========== 方案B：API配置（仅当 NLP_MODE="api" 时生效）==========
# 支持任何兼容OpenAI接口格式的API（通义千问/DeepSeek/Moonshot/GLM等）
API_PROVIDER = "deepseek"       # 当前使用的API提供商名称（仅用于日志标识）

# --- DeepSeek（推荐，注册送500万token免费额度）---
API_BASE_URL = "https://api.deepseek.com/v1/chat/completions"
API_KEY = "sk-你的密钥"          # https://platform.deepseek.com 获取
API_MODEL = "deepseek-chat"

# --- 通义千问（阿里，有免费额度）---
# API_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions"
# API_KEY = "sk-你的密钥"        # https://dashscope.console.aliyun.com 获取
# API_MODEL = "qwen-turbo"

# --- Moonshot / Kimi（月之暗面，有免费额度）---
# API_BASE_URL = "https://api.moonshot.cn/v1/chat/completions"
# API_KEY = "sk-你的密钥"        # https://platform.moonshot.cn 获取
# API_MODEL = "moonshot-v1-8k"

# --- 智谱GLM（智谱AI，有免费额度）---
# API_BASE_URL = "https://open.bigmodel.cn/api/paas/v4/chat/completions"
# API_KEY = "你的密钥"           # https://open.bigmodel.cn 获取
# API_MODEL = "glm-4-flash"     # flash版本免费

# ========== Flask配置 ==========
HOST = "0.0.0.0"
PORT = 5000
DEBUG = True
```

---

## 七、`requirements.txt`

```
flask==3.1.1
flask-socketio==5.5.1
eventlet==0.39.1
jieba==0.42.1
snownlp==0.12.3
requests==2.32.3
```

---

## 八、后端代码

### 8.1 Flask主应用 `app.py`

```python
import eventlet
eventlet.monkey_patch()

from flask import Flask, render_template
from flask_socketio import SocketIO, emit
from config import NLP_MODE, HOST, PORT, DEBUG

# 根据配置加载不同的分析器
if NLP_MODE == "api":
    from nlp.api_analyzer import ApiAnalyzer as Analyzer
    print("🌐 NLP模式: API增强方案")
else:
    from nlp.analyzer import LocalAnalyzer as Analyzer
    print("💻 NLP模式: 本地方案 (jieba + SnowNLP)")

app = Flask(__name__)
app.config["SECRET_KEY"] = "emotion-radio-secret"
socketio = SocketIO(app, cors_allowed_origins="*", async_mode="eventlet")

analyzer = Analyzer()

@app.route("/")
def index():
    return render_template("index.html")

@socketio.on("analyze_text")
def handle_text(data):
    text = data.get("text", "").strip()
    if not text:
        return
    result = analyzer.analyze(text)
    emit("emotion_result", result)

@socketio.on("connect")
def handle_connect():
    print("✅ 客户端已连接")

if __name__ == "__main__":
    socketio.run(app, host=HOST, port=PORT, debug=DEBUG)
```

### 8.2 本地分析器 `nlp/analyzer.py`（方案A）

```python
import jieba
from snownlp import SnowNLP
from .imagery_dict import IMAGERY_DICT, EMOTION_KEYWORDS
from .emotion_model import compute_arousal, compute_music_params, compute_visual_params

class LocalAnalyzer:
    """本地文本情绪分析器（jieba + SnowNLP + 词典）"""

    def __init__(self):
        for word in IMAGERY_DICT:
            jieba.add_word(word)
        for word in EMOTION_KEYWORDS:
            jieba.add_word(word)
        print(f"📚 词典已加载: 意象词 {len(IMAGERY_DICT)} 条, 情绪词 {len(EMOTION_KEYWORDS)} 条")

    def analyze(self, text: str) -> dict:
        # 1. SnowNLP情感分析 → 效价valence
        snow = SnowNLP(text)
        valence = snow.sentiments

        # 2. jieba分词
        words = list(jieba.cut(text))

        # 3. 意象词典匹配
        matched = []
        for w in words:
            if w in IMAGERY_DICT:
                matched.append({"word": w, **IMAGERY_DICT[w]})

        # 4. 情绪关键词修正valence
        for w in words:
            if w in EMOTION_KEYWORDS:
                valence = max(0, min(1, valence + EMOTION_KEYWORDS[w]))

        # 5. 计算唤醒度
        arousal = compute_arousal(text, words, matched)

        # 6~7. 生成音乐/视觉参数
        music = compute_music_params(valence, arousal, matched)
        visual = compute_visual_params(valence, arousal, matched)

        return {
            "text": text,
            "valence": round(valence, 3),
            "arousal": round(arousal, 3),
            "imageries": [m["word"] for m in matched],
            "music": music,
            "visual": visual,
        }
```

### 8.3 API增强分析器 `nlp/api_analyzer.py`（方案B）

```python
"""
API增强方案 —— 使用大语言模型进行更精准的情绪与意象分析
兼容所有OpenAI格式接口：DeepSeek / 通义千问 / Moonshot / GLM 等
"""

import json
import requests
from config import API_BASE_URL, API_KEY, API_MODEL
from .emotion_model import compute_music_params, compute_visual_params

SYSTEM_PROMPT = """你是一个文本情绪分析引擎。给定一段中文文字，你需要输出严格的JSON格式分析结果，不要输出任何其他内容。

JSON格式要求：
{
    "valence": 0.0到1.0之间的浮点数, // 情感效价：0=极消极, 0.5=中性, 1=极积极
    "arousal": 0.0到1.0之间的浮点数, // 唤醒度/情感强度：0=极平静, 1=极激动
    "imageries": ["意象1", "意象2"], // 文字中包含的自然意象/场景关键词
    "dominant_emotion": "情绪标签",   // 主导情绪：喜悦/悲伤/愤怒/恐惧/平静/思念/孤独/温暖/希望/绝望
    "suggested_instrument": "乐器",   // 最适合的乐器：piano/strings/flute/guitar/synth_pad/celesta/bells
    "suggested_effects": ["效果1"]    // 建议的视觉效果：rain/snow/fire/stars/petals/leaves/wave/moonlight/sunlight/storm/sparkle/drift/fog/rainbow/wind/ripple/glow/aurora/smoke
}

注意：
- imageries只提取具体的自然/场景意象词（如雨、月、花、海），不要提取抽象情绪词
- 仅输出JSON，不要加markdown代码块标记，不要加任何解释文字"""


class ApiAnalyzer:
    """基于大语言模型API的文本分析器"""

    def __init__(self):
        print(f"🌐 API分析器已初始化: {API_MODEL} @ {API_BASE_URL}")

    def analyze(self, text: str) -> dict:
        try:
            ai_result = self._call_api(text)
        except Exception as e:
            print(f"⚠️ API调用失败，回退到基础分析: {e}")
            ai_result = self._fallback_analyze(text)

        valence = ai_result.get("valence", 0.5)
        arousal = ai_result.get("arousal", 0.5)
        imageries = ai_result.get("imageries", [])

        # 构造意象数据（用于compute_music/visual_params）
        matched = []
        for img in imageries:
            matched.append({
                "word": img,
                "instrument": ai_result.get("suggested_instrument", "piano"),
                "effect": ai_result.get("suggested_effects", ["drift"])[0] if ai_result.get("suggested_effects") else "drift",
                "arousal": arousal,
            })

        # 如果API返回了建议的视觉效果，直接使用
        suggested_effects = ai_result.get("suggested_effects", [])

        music = compute_music_params(valence, arousal, matched)
        visual = compute_visual_params(valence, arousal, matched)

        # 用API建议的效果覆盖默认效果
        if suggested_effects:
            visual["particle_effects"] = suggested_effects[:3]

        # 用API建议的乐器覆盖
        if ai_result.get("suggested_instrument"):
            music["instrument"] = ai_result["suggested_instrument"]

        return {
            "text": text,
            "valence": round(valence, 3),
            "arousal": round(arousal, 3),
            "imageries": imageries,
            "music": music,
            "visual": visual,
        }

    def _call_api(self, text: str) -> dict:
        headers = {
            "Content-Type": "application/json",
            "Authorization": f"Bearer {API_KEY}",
        }
        payload = {
            "model": API_MODEL,
            "messages": [
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": text},
            ],
            "temperature": 0.3,
            "max_tokens": 300,
        }

        resp = requests.post(API_BASE_URL, headers=headers, json=payload, timeout=10)
        resp.raise_for_status()
        content = resp.json()["choices"][0]["message"]["content"].strip()

        # 清理可能的markdown代码块标记
        content = content.replace("```json", "").replace("```", "").strip()

        return json.loads(content)

    def _fallback_analyze(self, text: str) -> dict:
        """API失败时的简单回退"""
        return {
            "valence": 0.5,
            "arousal": 0.5,
            "imageries": [],
            "dominant_emotion": "平静",
            "suggested_instrument": "piano",
            "suggested_effects": ["drift"],
        }
```

### 8.4 情绪参数计算 `nlp/emotion_model.py`

```python
"""将原始情感分析结果转化为音乐/视觉引擎可用的参数"""


def compute_arousal(text: str, words: list, imageries: list) -> float:
    arousal = 0.5
    arousal += text.count("！") * 0.08 + text.count("!") * 0.08
    arousal += text.count("？") * 0.05 + text.count("?") * 0.05
    arousal -= text.count("…") * 0.05 + text.count("...") * 0.05

    if imageries:
        avg = sum(img.get("arousal", 0.5) for img in imageries) / len(imageries)
        arousal = arousal * 0.4 + avg * 0.6

    if len(text) < 6:
        arousal += 0.1

    return max(0.0, min(1.0, arousal))


def compute_music_params(valence: float, arousal: float, imageries: list) -> dict:
    # 调式
    if valence > 0.65:
        scale, root = "pentatonic_major", "C"
    elif valence > 0.45:
        scale, root = "major", "F"
    elif valence > 0.3:
        scale, root = "pentatonic_minor", "A"
    else:
        scale, root = "minor", "D"

    # 速度
    tempo = int(60 + arousal * 100)

    # 乐器
    instrument = "piano"
    for img in imageries:
        if "instrument" in img:
            instrument = img["instrument"]
            break

    # 混响
    reverb = 0.3
    spacious = {"星空", "大海", "宇宙", "远方", "天空", "旷野", "沙漠", "深渊", "月", "海", "太空", "银河", "苍穹"}
    for img in imageries:
        if img.get("word") in spacious:
            reverb = 0.8
            break

    velocity = 0.3 + arousal * 0.7
    octave = 5 if arousal > 0.7 else (3 if arousal < 0.3 else 4)
    density = 0.2 + arousal * 0.6

    return {
        "scale": scale, "root": root, "tempo": tempo,
        "instrument": instrument, "reverb": round(reverb, 2),
        "velocity": round(velocity, 2), "octave": octave,
        "density": round(density, 2),
    }


def compute_visual_params(valence: float, arousal: float, imageries: list) -> dict:
    # 背景HSL
    if valence > 0.6:
        h = 30 + valence * 30
    elif valence > 0.4:
        h = 260 - valence * 60
    else:
        h = 210 + (1 - valence) * 30
    s = 30 + arousal * 40
    l = 6 + valence * 12

    # 粒子效果
    effects = []
    for img in imageries:
        eff = img.get("effect")
        if eff and eff not in effects:
            effects.append(eff)
    if not effects:
        if valence > 0.6 and arousal > 0.5:
            effects = ["sparkle"]
        elif valence > 0.6:
            effects = ["float"]
        elif valence < 0.4 and arousal > 0.5:
            effects = ["storm"]
        else:
            effects = ["drift"]

    speed = 0.3 + arousal * 1.7
    count = int(30 + arousal * 170)
    palette = _palette(valence, arousal, imageries)
    glow = 0.2 + arousal * 0.6
    transition = 3.0 - arousal * 2.0

    return {
        "background": {"h": round(h), "s": round(s), "l": round(l)},
        "particle_effects": effects,
        "particle_speed": round(speed, 2),
        "particle_count": count,
        "color_palette": palette,
        "glow_intensity": round(glow, 2),
        "transition_duration": round(transition, 1),
    }


def _palette(valence, arousal, imageries):
    from_img = [img["color"] for img in imageries if "color" in img]
    if len(from_img) >= 3:
        return from_img[:3]
    if valence > 0.7:
        base = ["#FFD700", "#FFA07A", "#FFFACD"]
    elif valence > 0.5:
        base = ["#87CEEB", "#98FB98", "#DDA0DD"]
    elif valence > 0.3:
        base = ["#6A5ACD", "#708090", "#B0C4DE"]
    else:
        base = ["#2F4F4F", "#483D8B", "#191970"]
    for i, c in enumerate(from_img):
        if i < 3:
            base[i] = c
    return base
```

### 8.5 意象-情绪映射词典 `nlp/imagery_dict.py`（300+词条完整版）

```python
"""
意象-情绪映射词典（完整版 300+ 词条）

字段说明：
- valence_adj: 对情感效价的修正值
- arousal: 该意象的固有唤醒度
- effect: 触发的视觉粒子效果名称
- color: 代表色（hex）
- instrument: 适合的乐器音色
"""

IMAGERY_DICT = {

    # ==================== 天气与气象（40+）====================
    "雨":       {"valence_adj": -0.10, "arousal": 0.40, "effect": "rain",       "color": "#4A90D9", "instrument": "piano"},
    "大雨":     {"valence_adj": -0.15, "arousal": 0.70, "effect": "heavy_rain", "color": "#2C3E73", "instrument": "piano"},
    "小雨":     {"valence_adj": -0.05, "arousal": 0.20, "effect": "light_rain", "color": "#7EB6D9", "instrument": "piano"},
    "细雨":     {"valence_adj": -0.03, "arousal": 0.15, "effect": "light_rain", "color": "#8EC4E8", "instrument": "piano"},
    "暴雨":     {"valence_adj": -0.20, "arousal": 0.90, "effect": "storm",      "color": "#1A1A3E", "instrument": "strings"},
    "雷阵雨":   {"valence_adj": -0.18, "arousal": 0.85, "effect": "storm",      "color": "#2A2A50", "instrument": "strings"},
    "梅雨":     {"valence_adj": -0.08, "arousal": 0.25, "effect": "rain",       "color": "#6699AA", "instrument": "piano"},
    "淋雨":     {"valence_adj": -0.15, "arousal": 0.50, "effect": "heavy_rain", "color": "#3A5A8C", "instrument": "piano"},
    "雨滴":     {"valence_adj": -0.03, "arousal": 0.20, "effect": "light_rain", "color": "#7CAAD4", "instrument": "celesta"},
    "雪":       {"valence_adj":  0.05, "arousal": 0.15, "effect": "snow",       "color": "#E8E8F0", "instrument": "celesta"},
    "大雪":     {"valence_adj":  0.00, "arousal": 0.30, "effect": "heavy_snow", "color": "#C8C8E0", "instrument": "celesta"},
    "小雪":     {"valence_adj":  0.08, "arousal": 0.10, "effect": "snow",       "color": "#F0F0F8", "instrument": "celesta"},
    "飘雪":     {"valence_adj":  0.05, "arousal": 0.12, "effect": "snow",       "color": "#E0E0F0", "instrument": "celesta"},
    "暴风雪":   {"valence_adj": -0.15, "arousal": 0.85, "effect": "storm",      "color": "#9090B0", "instrument": "strings"},
    "雪花":     {"valence_adj":  0.10, "arousal": 0.10, "effect": "snow",       "color": "#F5F5FF", "instrument": "celesta"},
    "风":       {"valence_adj": -0.05, "arousal": 0.50, "effect": "wind",       "color": "#A8C8C8", "instrument": "flute"},
    "微风":     {"valence_adj":  0.10, "arousal": 0.20, "effect": "breeze",     "color": "#C8E8D8", "instrument": "flute"},
    "清风":     {"valence_adj":  0.12, "arousal": 0.20, "effect": "breeze",     "color": "#B8E0C8", "instrument": "flute"},
    "暴风":     {"valence_adj": -0.20, "arousal": 0.90, "effect": "storm",      "color": "#3D3D5C", "instrument": "strings"},
    "狂风":     {"valence_adj": -0.18, "arousal": 0.85, "effect": "storm",      "color": "#4A4A6A", "instrument": "strings"},
    "寒风":     {"valence_adj": -0.15, "arousal": 0.60, "effect": "wind",       "color": "#8090A8", "instrument": "strings"},
    "秋风":     {"valence_adj": -0.05, "arousal": 0.30, "effect": "leaves",     "color": "#C8A848", "instrument": "guitar"},
    "春风":     {"valence_adj":  0.15, "arousal": 0.30, "effect": "breeze",     "color": "#A8D8A8", "instrument": "flute"},
    "雷":       {"valence_adj": -0.15, "arousal": 0.95, "effect": "lightning",  "color": "#FFFF66", "instrument": "drums"},
    "雷声":     {"valence_adj": -0.12, "arousal": 0.90, "effect": "lightning",  "color": "#FFFF88", "instrument": "drums"},
    "闪电":     {"valence_adj": -0.10, "arousal": 0.95, "effect": "lightning",  "color": "#FFFF99", "instrument": "drums"},
    "阳光":     {"valence_adj":  0.20, "arousal": 0.50, "effect": "sunlight",  "color": "#FFD700", "instrument": "guitar"},
    "晴":       {"valence_adj":  0.15, "arousal": 0.40, "effect": "sunlight",  "color": "#FFE44D", "instrument": "guitar"},
    "晴天":     {"valence_adj":  0.15, "arousal": 0.40, "effect": "sunlight",  "color": "#FFE44D", "instrument": "guitar"},
    "晴朗":     {"valence_adj":  0.18, "arousal": 0.45, "effect": "sunlight",  "color": "#FFDA33", "instrument": "guitar"},
    "云":       {"valence_adj":  0.00, "arousal": 0.20, "effect": "clouds",    "color": "#D3D3E8", "instrument": "synth_pad"},
    "白云":     {"valence_adj":  0.10, "arousal": 0.15, "effect": "clouds",    "color": "#E8E8F8", "instrument": "synth_pad"},
    "乌云":     {"valence_adj": -0.15, "arousal": 0.50, "effect": "clouds",    "color": "#4A4A6A", "instrument": "strings"},
    "云层":     {"valence_adj":  0.00, "arousal": 0.20, "effect": "clouds",    "color": "#B8B8D0", "instrument": "synth_pad"},
    "雾":       {"valence_adj": -0.05, "arousal": 0.15, "effect": "fog",       "color": "#B8B8C8", "instrument": "synth_pad"},
    "浓雾":     {"valence_adj": -0.10, "arousal": 0.20, "effect": "fog",       "color": "#909098", "instrument": "synth_pad"},
    "薄雾":     {"valence_adj":  0.00, "arousal": 0.10, "effect": "fog",       "color": "#D0D0D8", "instrument": "synth_pad"},
    "晨雾":     {"valence_adj":  0.05, "arousal": 0.12, "effect": "fog",       "color": "#C8C8D8", "instrument": "flute"},
    "彩虹":     {"valence_adj":  0.30, "arousal": 0.60, "effect": "rainbow",   "color": "#FF69B4", "instrument": "bells"},
    "霞":       {"valence_adj":  0.15, "arousal": 0.30, "effect": "glow",      "color": "#FF7F50", "instrument": "strings"},
    "晚霞":     {"valence_adj":  0.10, "arousal": 0.30, "effect": "sunset",    "color": "#FF6347", "instrument": "strings"},
    "朝霞":     {"valence_adj":  0.20, "arousal": 0.40, "effect": "sunrise",   "color": "#FF8C00", "instrument": "flute"},
    "霜":       {"valence_adj": -0.08, "arousal": 0.15, "effect": "snow",      "color": "#D8D8E8", "instrument": "celesta"},
    "露":       {"valence_adj":  0.08, "arousal": 0.10, "effect": "sparkle",   "color": "#C8E8E8", "instrument": "celesta"},
    "露珠":     {"valence_adj":  0.10, "arousal": 0.10, "effect": "sparkle",   "color": "#B8E0E0", "instrument": "celesta"},
    "冰":       {"valence_adj": -0.10, "arousal": 0.20, "effect": "sparkle",   "color": "#A8D8F0", "instrument": "celesta"},
    "冰雪":     {"valence_adj": -0.05, "arousal": 0.25, "effect": "snow",      "color": "#C0D8F0", "instrument": "celesta"},

    # ==================== 天体与宇宙（25+）====================
    "太阳":     {"valence_adj":  0.20, "arousal": 0.50, "effect": "sunlight",  "color": "#FFA500", "instrument": "guitar"},
    "日出":     {"valence_adj":  0.25, "arousal": 0.50, "effect": "sunrise",   "color": "#FF4500", "instrument": "strings"},
    "日落":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "sunset",    "color": "#FF6347", "instrument": "strings"},
    "夕阳":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "sunset",    "color": "#FF5733", "instrument": "strings"},
    "落日":     {"valence_adj":  0.03, "arousal": 0.28, "effect": "sunset",    "color": "#E8532A", "instrument": "strings"},
    "月":       {"valence_adj":  0.00, "arousal": 0.15, "effect": "moonlight", "color": "#FFFACD", "instrument": "piano"},
    "月亮":     {"valence_adj":  0.00, "arousal": 0.15, "effect": "moonlight", "color": "#FFFACD", "instrument": "piano"},
    "月光":     {"valence_adj":  0.05, "arousal": 0.10, "effect": "moonlight", "color": "#F0E68C", "instrument": "piano"},
    "明月":     {"valence_adj":  0.05, "arousal": 0.15, "effect": "moonlight", "color": "#FAFAC8", "instrument": "piano"},
    "弯月":     {"valence_adj":  0.03, "arousal": 0.12, "effect": "moonlight", "color": "#F0E8B0", "instrument": "piano"},
    "满月":     {"valence_adj":  0.08, "arousal": 0.18, "effect": "moonlight", "color": "#FFFFF0", "instrument": "piano"},
    "新月":     {"valence_adj": -0.02, "arousal": 0.10, "effect": "moonlight", "color": "#E8E0A8", "instrument": "piano"},
    "星":       {"valence_adj":  0.10, "arousal": 0.20, "effect": "stars",     "color": "#FFD700", "instrument": "celesta"},
    "星星":     {"valence_adj":  0.15, "arousal": 0.20, "effect": "stars",     "color": "#FFD700", "instrument": "celesta"},
    "星空":     {"valence_adj":  0.10, "arousal": 0.20, "effect": "stars",     "color": "#191970", "instrument": "synth_pad"},
    "星辰":     {"valence_adj":  0.10, "arousal": 0.22, "effect": "stars",     "color": "#2A2A6A", "instrument": "synth_pad"},
    "繁星":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "stars",     "color": "#FFE066", "instrument": "celesta"},
    "流星":     {"valence_adj":  0.20, "arousal": 0.60, "effect": "shooting_star","color": "#FFD700","instrument": "celesta"},
    "银河":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "stars",     "color": "#B8C8FF", "instrument": "synth_pad"},
    "宇宙":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "stars",     "color": "#0B0B45", "instrument": "synth_pad"},
    "太空":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "stars",     "color": "#0A0A3A", "instrument": "synth_pad"},
    "天空":     {"valence_adj":  0.10, "arousal": 0.30, "effect": "clouds",    "color": "#87CEEB", "instrument": "synth_pad"},
    "苍穹":     {"valence_adj":  0.05, "arousal": 0.28, "effect": "clouds",    "color": "#6A9EC8", "instrument": "synth_pad"},

    # ==================== 水域与海洋（20+）====================
    "水":       {"valence_adj":  0.05, "arousal": 0.30, "effect": "ripple",    "color": "#4682B4", "instrument": "piano"},
    "清水":     {"valence_adj":  0.10, "arousal": 0.20, "effect": "ripple",    "color": "#5CACEE", "instrument": "piano"},
    "河":       {"valence_adj":  0.05, "arousal": 0.35, "effect": "flow",      "color": "#5B9BD5", "instrument": "piano"},
    "河流":     {"valence_adj":  0.05, "arousal": 0.35, "effect": "flow",      "color": "#5B9BD5", "instrument": "piano"},
    "小河":     {"valence_adj":  0.10, "arousal": 0.25, "effect": "flow",      "color": "#6CB4E0", "instrument": "piano"},
    "溪":       {"valence_adj":  0.12, "arousal": 0.25, "effect": "flow",      "color": "#6CB4E0", "instrument": "celesta"},
    "溪流":     {"valence_adj":  0.12, "arousal": 0.25, "effect": "flow",      "color": "#6CB4E0", "instrument": "celesta"},
    "小溪":     {"valence_adj":  0.15, "arousal": 0.20, "effect": "flow",      "color": "#78C0E8", "instrument": "celesta"},
    "泉":       {"valence_adj":  0.12, "arousal": 0.20, "effect": "ripple",    "color": "#70B8D8", "instrument": "celesta"},
    "泉水":     {"valence_adj":  0.12, "arousal": 0.20, "effect": "ripple",    "color": "#70B8D8", "instrument": "celesta"},
    "瀑布":     {"valence_adj":  0.10, "arousal": 0.75, "effect": "heavy_rain","color": "#3A8CC0", "instrument": "strings"},
    "海":       {"valence_adj":  0.10, "arousal": 0.50, "effect": "wave",      "color": "#006994", "instrument": "synth_pad"},
    "大海":     {"valence_adj":  0.10, "arousal": 0.50, "effect": "wave",      "color": "#006994", "instrument": "synth_pad"},
    "海洋":     {"valence_adj":  0.10, "arousal": 0.50, "effect": "wave",      "color": "#005580", "instrument": "synth_pad"},
    "海浪":     {"valence_adj":  0.05, "arousal": 0.60, "effect": "wave",      "color": "#1E90FF", "instrument": "synth_pad"},
    "波浪":     {"valence_adj":  0.05, "arousal": 0.55, "effect": "wave",      "color": "#2898E8", "instrument": "synth_pad"},
    "潮汐":     {"valence_adj":  0.00, "arousal": 0.40, "effect": "wave",      "color": "#2070A0", "instrument": "synth_pad"},
    "湖":       {"valence_adj":  0.10, "arousal": 0.15, "effect": "ripple",    "color": "#5F9EA0", "instrument": "piano"},
    "湖泊":     {"valence_adj":  0.10, "arousal": 0.15, "effect": "ripple",    "color": "#5F9EA0", "instrument": "piano"},
    "湖面":     {"valence_adj":  0.08, "arousal": 0.12, "effect": "ripple",    "color": "#68A8A8", "instrument": "piano"},
    "池塘":     {"valence_adj":  0.08, "arousal": 0.10, "effect": "ripple",    "color": "#70A8A0", "instrument": "piano"},
    "深海":     {"valence_adj": -0.05, "arousal": 0.35, "effect": "wave",      "color": "#003060", "instrument": "synth_pad"},

    # ==================== 山岳与大地（20+）====================
    "山":       {"valence_adj":  0.05, "arousal": 0.30, "effect": "float",     "color": "#556B2F", "instrument": "strings"},
    "高山":     {"valence_adj":  0.05, "arousal": 0.40, "effect": "float",     "color": "#2F4F2F", "instrument": "strings"},
    "山峰":     {"valence_adj":  0.08, "arousal": 0.45, "effect": "float",     "color": "#3A5A3A", "instrument": "strings"},
    "群山":     {"valence_adj":  0.05, "arousal": 0.35, "effect": "float",     "color": "#4A6A4A", "instrument": "strings"},
    "远山":     {"valence_adj":  0.00, "arousal": 0.20, "effect": "fog",       "color": "#8899AA", "instrument": "synth_pad"},
    "山谷":     {"valence_adj":  0.05, "arousal": 0.20, "effect": "fog",       "color": "#5A7A5A", "instrument": "flute"},
    "峡谷":     {"valence_adj": -0.03, "arousal": 0.35, "effect": "wind",      "color": "#6A5A4A", "instrument": "strings"},
    "悬崖":     {"valence_adj": -0.10, "arousal": 0.65, "effect": "wind",      "color": "#5A4A3A", "instrument": "strings"},
    "大地":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "float",     "color": "#8B7355", "instrument": "strings"},
    "田野":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "breeze",    "color": "#90B860", "instrument": "guitar"},
    "平原":     {"valence_adj":  0.10, "arousal": 0.20, "effect": "breeze",    "color": "#A0C870", "instrument": "guitar"},
    "草原":     {"valence_adj":  0.15, "arousal": 0.30, "effect": "breeze",    "color": "#78B850", "instrument": "flute"},
    "旷野":     {"valence_adj": -0.05, "arousal": 0.30, "effect": "wind",      "color": "#A0A060", "instrument": "flute"},
    "荒野":     {"valence_adj": -0.10, "arousal": 0.35, "effect": "dust",      "color": "#B0A070", "instrument": "strings"},
    "沙漠":     {"valence_adj": -0.05, "arousal": 0.30, "effect": "dust",      "color": "#EDC9AF", "instrument": "flute"},
    "戈壁":     {"valence_adj": -0.10, "arousal": 0.30, "effect": "dust",      "color": "#C8A878", "instrument": "flute"},
    "沙丘":     {"valence_adj": -0.03, "arousal": 0.25, "effect": "dust",      "color": "#D8B888", "instrument": "flute"},
    "泥土":     {"valence_adj":  0.00, "arousal": 0.15, "effect": "float",     "color": "#8B6914", "instrument": "guitar"},
    "石头":     {"valence_adj":  0.00, "arousal": 0.20, "effect": "float",     "color": "#808080", "instrument": "piano"},

    # ==================== 植物与花卉（35+）====================
    "花":       {"valence_adj":  0.20, "arousal": 0.35, "effect": "petals",    "color": "#FFB6C1", "instrument": "bells"},
    "鲜花":     {"valence_adj":  0.22, "arousal": 0.35, "effect": "petals",    "color": "#FF9EAF", "instrument": "bells"},
    "花朵":     {"valence_adj":  0.20, "arousal": 0.30, "effect": "petals",    "color": "#FFB6C1", "instrument": "bells"},
    "花瓣":     {"valence_adj":  0.18, "arousal": 0.25, "effect": "petals",    "color": "#FFAABB", "instrument": "bells"},
    "花香":     {"valence_adj":  0.22, "arousal": 0.30, "effect": "petals",    "color": "#FFD0E0", "instrument": "flute"},
    "樱花":     {"valence_adj":  0.25, "arousal": 0.30, "effect": "petals",    "color": "#FFB7C5", "instrument": "bells"},
    "桃花":     {"valence_adj":  0.22, "arousal": 0.30, "effect": "petals",    "color": "#FF8FAF", "instrument": "bells"},
    "梅花":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "petals",    "color": "#FF6088", "instrument": "flute"},
    "荷花":     {"valence_adj":  0.15, "arousal": 0.20, "effect": "petals",    "color": "#F8C8DC", "instrument": "flute"},
    "莲花":     {"valence_adj":  0.18, "arousal": 0.18, "effect": "petals",    "color": "#F0C0D0", "instrument": "flute"},
    "玫瑰":     {"valence_adj":  0.20, "arousal": 0.40, "effect": "petals",    "color": "#FF1744", "instrument": "strings"},
    "菊花":     {"valence_adj":  0.05, "arousal": 0.20, "effect": "petals",    "color": "#FFD700", "instrument": "guitar"},
    "向日葵":   {"valence_adj":  0.25, "arousal": 0.45, "effect": "sunlight",  "color": "#FFD700", "instrument": "guitar"},
    "兰花":     {"valence_adj":  0.15, "arousal": 0.15, "effect": "float",     "color": "#DA70D6", "instrument": "flute"},
    "百合":     {"valence_adj":  0.18, "arousal": 0.20, "effect": "petals",    "color": "#FFFFFF", "instrument": "bells"},
    "牡丹":     {"valence_adj":  0.20, "arousal": 0.35, "effect": "petals",    "color": "#FF3366", "instrument": "strings"},
    "薰衣草":   {"valence_adj":  0.15, "arousal": 0.15, "effect": "float",     "color": "#B57EDC", "instrument": "flute"},
    "丁香":     {"valence_adj":  0.12, "arousal": 0.15, "effect": "float",     "color": "#C8A2C8", "instrument": "flute"},
    "树":       {"valence_adj":  0.05, "arousal": 0.20, "effect": "leaves",    "color": "#228B22", "instrument": "guitar"},
    "大树":     {"valence_adj":  0.08, "arousal": 0.22, "effect": "leaves",    "color": "#1A7A1A", "instrument": "guitar"},
    "古树":     {"valence_adj":  0.00, "arousal": 0.18, "effect": "leaves",    "color": "#3A5A2A", "instrument": "strings"},
    "柳树":     {"valence_adj":  0.05, "arousal": 0.18, "effect": "breeze",    "color": "#6B8E23", "instrument": "flute"},
    "松树":     {"valence_adj":  0.05, "arousal": 0.20, "effect": "wind",      "color": "#2E5A2E", "instrument": "strings"},
    "竹":       {"valence_adj":  0.10, "arousal": 0.20, "effect": "breeze",    "color": "#3CB371", "instrument": "flute"},
    "竹林":     {"valence_adj":  0.10, "arousal": 0.22, "effect": "breeze",    "color": "#2E8B57", "instrument": "flute"},
    "森林":     {"valence_adj":  0.10, "arousal": 0.25, "effect": "leaves",    "color": "#006400", "instrument": "guitar"},
    "树林":     {"valence_adj":  0.08, "arousal": 0.22, "effect": "leaves",    "color": "#1A6A1A", "instrument": "guitar"},
    "草":       {"valence_adj":  0.10, "arousal": 0.20, "effect": "breeze",    "color": "#7CFC00", "instrument": "flute"},
    "青草":     {"valence_adj":  0.12, "arousal": 0.22, "effect": "breeze",    "color": "#68D838", "instrument": "flute"},
    "落叶":     {"valence_adj": -0.10, "arousal": 0.20, "effect": "leaves",    "color": "#D2691E", "instrument": "guitar"},
    "枯叶":     {"valence_adj": -0.12, "arousal": 0.18, "effect": "leaves",    "color": "#A0522D", "instrument": "guitar"},
    "绿叶":     {"valence_adj":  0.15, "arousal": 0.22, "effect": "leaves",    "color": "#32CD32", "instrument": "guitar"},
    "叶子":     {"valence_adj":  0.05, "arousal": 0.18, "effect": "leaves",    "color": "#6AAF28", "instrument": "guitar"},
    "藤蔓":     {"valence_adj":  0.05, "arousal": 0.15, "effect": "float",     "color": "#4A8A2A", "instrument": "guitar"},
    "苔藓":     {"valence_adj":  0.03, "arousal": 0.10, "effect": "float",     "color": "#5A8A3A", "instrument": "piano"},
    "种子":     {"valence_adj":  0.10, "arousal": 0.15, "effect": "float",     "color": "#8B6914", "instrument": "celesta"},
    "果实":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "sparkle",   "color": "#FF6347", "instrument": "bells"},

    # ==================== 火/光/暗/影（25+）====================
    "火":       {"valence_adj": -0.05, "arousal": 0.85, "effect": "fire",      "color": "#FF4500", "instrument": "drums"},
    "火焰":     {"valence_adj": -0.05, "arousal": 0.90, "effect": "fire",      "color": "#FF4500", "instrument": "drums"},
    "篝火":     {"valence_adj":  0.15, "arousal": 0.45, "effect": "fire",      "color": "#FF6600", "instrument": "guitar"},
    "烈火":     {"valence_adj": -0.10, "arousal": 0.95, "effect": "fire",      "color": "#FF2200", "instrument": "drums"},
    "火花":     {"valence_adj":  0.10, "arousal": 0.60, "effect": "sparkle",   "color": "#FFA500", "instrument": "celesta"},
    "烟花":     {"valence_adj":  0.30, "arousal": 0.85, "effect": "firework",  "color": "#FF69B4", "instrument": "bells"},
    "焰火":     {"valence_adj":  0.28, "arousal": 0.80, "effect": "firework",  "color": "#FF6EB4", "instrument": "bells"},
    "烛光":     {"valence_adj":  0.05, "arousal": 0.15, "effect": "candle",    "color": "#FFD700", "instrument": "piano"},
    "灯":       {"valence_adj":  0.05, "arousal": 0.20, "effect": "glow",      "color": "#FAFAD2", "instrument": "piano"},
    "灯火":     {"valence_adj":  0.08, "arousal": 0.25, "effect": "glow",      "color": "#FFE4B5", "instrument": "piano"},
    "灯光":     {"valence_adj":  0.05, "arousal": 0.22, "effect": "glow",      "color": "#FFFACD", "instrument": "piano"},
    "路灯":     {"valence_adj":  0.00, "arousal": 0.18, "effect": "glow",      "color": "#FFE8A0", "instrument": "piano"},
    "霓虹":     {"valence_adj":  0.10, "arousal": 0.55, "effect": "sparkle",   "color": "#FF00FF", "instrument": "synth_pad"},
    "光":       {"valence_adj":  0.15, "arousal": 0.40, "effect": "glow",      "color": "#FFFACD", "instrument": "bells"},
    "光芒":     {"valence_adj":  0.20, "arousal": 0.60, "effect": "burst",     "color": "#FFD700", "instrument": "bells"},
    "光线":     {"valence_adj":  0.12, "arousal": 0.35, "effect": "sunlight",  "color": "#FFE8A0", "instrument": "bells"},
    "光影":     {"valence_adj":  0.05, "arousal": 0.25, "effect": "glow",      "color": "#C8C8A0", "instrument": "piano"},
    "曙光":     {"valence_adj":  0.25, "arousal": 0.50, "effect": "sunrise",   "color": "#FF8C00", "instrument": "strings"},
    "暮光":     {"valence_adj":  0.00, "arousal": 0.25, "effect": "sunset",    "color": "#9370DB", "instrument": "synth_pad"},
    "极光":     {"valence_adj":  0.20, "arousal": 0.40, "effect": "aurora",    "color": "#00FF88", "instrument": "synth_pad"},
    "黑暗":     {"valence_adj": -0.20, "arousal": 0.40, "effect": "darkness",  "color": "#0D0D1A", "instrument": "synth_pad"},
    "黑夜":     {"valence_adj": -0.10, "arousal": 0.25, "effect": "darkness",  "color": "#0A0A20", "instrument": "synth_pad"},
    "阴影":     {"valence_adj": -0.10, "arousal": 0.30, "effect": "darkness",  "color": "#2A2A3A", "instrument": "synth_pad"},
    "影子":     {"valence_adj": -0.05, "arousal": 0.25, "effect": "darkness",  "color": "#3A3A4A", "instrument": "piano"},
    "烟":       {"valence_adj": -0.05, "arousal": 0.25, "effect": "smoke",     "color": "#888888", "instrument": "synth_pad"},
    "炊烟":     {"valence_adj":  0.10, "arousal": 0.15, "effect": "smoke",     "color": "#A0A0A0", "instrument": "flute"},
    "烟雾":     {"valence_adj": -0.05, "arousal": 0.20, "effect": "smoke",     "color": "#808080", "instrument": "synth_pad"},

    # ==================== 季节与时间（20+）====================
    "春":       {"valence_adj":  0.20, "arousal": 0.40, "effect": "petals",    "color": "#98FB98", "instrument": "flute"},
    "春天":     {"valence_adj":  0.20, "arousal": 0.40, "effect": "petals",    "color": "#98FB98", "instrument": "flute"},
    "春日":     {"valence_adj":  0.22, "arousal": 0.42, "effect": "petals",    "color": "#90F890", "instrument": "flute"},
    "初春":     {"valence_adj":  0.18, "arousal": 0.35, "effect": "breeze",    "color": "#A8E8A8", "instrument": "flute"},
    "夏":       {"valence_adj":  0.10, "arousal": 0.60, "effect": "sunlight",  "color": "#FFD700", "instrument": "guitar"},
    "夏天":     {"valence_adj":  0.10, "arousal": 0.60, "effect": "sunlight",  "color": "#FFD700", "instrument": "guitar"},
    "盛夏":     {"valence_adj":  0.08, "arousal": 0.70, "effect": "sunlight",  "color": "#FF8C00", "instrument": "guitar"},
    "秋":       {"valence_adj": -0.05, "arousal": 0.30, "effect": "leaves",    "color": "#DAA520", "instrument": "piano"},
    "秋天":     {"valence_adj": -0.05, "arousal": 0.30, "effect": "leaves",    "color": "#DAA520", "instrument": "piano"},
    "深秋":     {"valence_adj": -0.10, "arousal": 0.28, "effect": "leaves",    "color": "#B8860B", "instrument": "piano"},
    "金秋":     {"valence_adj":  0.10, "arousal": 0.35, "effect": "leaves",    "color": "#FFD700", "instrument": "guitar"},
    "冬":       {"valence_adj": -0.10, "arousal": 0.20, "effect": "snow",      "color": "#B0C4DE", "instrument": "celesta"},
    "冬天":     {"valence_adj": -0.10, "arousal": 0.20, "effect": "snow",      "color": "#B0C4DE", "instrument": "celesta"},
    "寒冬":     {"valence_adj": -0.15, "arousal": 0.25, "effect": "snow",      "color": "#8898B0", "instrument": "celesta"},
    "夜":       {"valence_adj": -0.05, "arousal": 0.20, "effect": "stars",     "color": "#191970", "instrument": "piano"},
    "夜晚":     {"valence_adj": -0.05, "arousal": 0.20, "effect": "stars",     "color": "#191970", "instrument": "piano"},
    "深夜":     {"valence_adj": -0.10, "arousal": 0.15, "effect": "stars",     "color": "#0D0D2B", "instrument": "synth_pad"},
    "午夜":     {"valence_adj": -0.10, "arousal": 0.18, "effect": "stars",     "color": "#0B0B28", "instrument": "synth_pad"},
    "凌晨":     {"valence_adj": -0.05, "arousal": 0.20, "effect": "moonlight", "color": "#1A1A40", "instrument": "piano"},
    "黎明":     {"valence_adj":  0.15, "arousal": 0.40, "effect": "sunrise",   "color": "#FF8C69", "instrument": "strings"},
    "拂晓":     {"valence_adj":  0.12, "arousal": 0.35, "effect": "sunrise",   "color": "#FF9980", "instrument": "flute"},
    "清晨":     {"valence_adj":  0.15, "arousal": 0.35, "effect": "sunlight",  "color": "#FFE4B5", "instrument": "flute"},
    "黄昏":     {"valence_adj": -0.05, "arousal": 0.25, "effect": "sunset",    "color": "#CD853F", "instrument": "guitar"},
    "傍晚":     {"valence_adj": -0.03, "arousal": 0.22, "effect": "sunset",    "color": "#D89050", "instrument": "guitar"},
    "正午":     {"valence_adj":  0.10, "arousal": 0.50, "effect": "sunlight",  "color": "#FFCC00", "instrument": "guitar"},

    # ==================== 动物（20+）====================
    "鸟":       {"valence_adj":  0.10, "arousal": 0.40, "effect": "float",     "color": "#87CEEB", "instrument": "flute"},
    "小鸟":     {"valence_adj":  0.15, "arousal": 0.35, "effect": "float",     "color": "#90D0F0", "instrument": "flute"},
    "飞鸟":     {"valence_adj":  0.12, "arousal": 0.50, "effect": "float",     "color": "#80C0E0", "instrument": "flute"},
    "鹰":       {"valence_adj":  0.05, "arousal": 0.60, "effect": "wind",      "color": "#5A4A3A", "instrument": "strings"},
    "燕子":     {"valence_adj":  0.15, "arousal": 0.40, "effect": "float",     "color": "#2A2A3A", "instrument": "flute"},
    "蝴蝶":     {"valence_adj":  0.20, "arousal": 0.30, "effect": "petals",    "color": "#DA70D6", "instrument": "bells"},
    "蜻蜓":     {"valence_adj":  0.15, "arousal": 0.25, "effect": "float",     "color": "#66CDAA", "instrument": "celesta"},
    "萤火虫":   {"valence_adj":  0.20, "arousal": 0.20, "effect": "firefly",   "color": "#CCFF00", "instrument": "celesta"},
    "蝉":       {"valence_adj":  0.00, "arousal": 0.35, "effect": "float",     "color": "#8B8B00", "instrument": "synth_pad"},
    "鱼":       {"valence_adj":  0.10, "arousal": 0.20, "effect": "ripple",    "color": "#4682B4", "instrument": "piano"},
    "金鱼":     {"valence_adj":  0.15, "arousal": 0.18, "effect": "ripple",    "color": "#FF6347", "instrument": "celesta"},
    "鲸":       {"valence_adj":  0.10, "arousal": 0.40, "effect": "wave",      "color": "#2A5A8A", "instrument": "synth_pad"},
    "鲸鱼":     {"valence_adj":  0.10, "arousal": 0.40, "effect": "wave",      "color": "#2A5A8A", "instrument": "synth_pad"},
    "猫":       {"valence_adj":  0.15, "arousal": 0.20, "effect": "float",     "color": "#DEB887", "instrument": "piano"},
    "小猫":     {"valence_adj":  0.20, "arousal": 0.22, "effect": "float",     "color": "#E0C8A0", "instrument": "celesta"},
    "狗":       {"valence_adj":  0.15, "arousal": 0.40, "effect": "sparkle",   "color": "#C8A060", "instrument": "guitar"},
    "马":       {"valence_adj":  0.10, "arousal": 0.55, "effect": "wind",      "color": "#8B4513", "instrument": "strings"},
    "白鸽":     {"valence_adj":  0.20, "arousal": 0.30, "effect": "float",     "color": "#F0F0F0", "instrument": "flute"},
    "鹤":       {"valence_adj":  0.10, "arousal": 0.25, "effect": "float",     "color": "#E8E8E8", "instrument": "flute"},
    "蛙":       {"valence_adj":  0.05, "arousal": 0.25, "effect": "ripple",    "color": "#3CB371", "instrument": "celesta"},
    "虫":       {"valence_adj":  0.00, "arousal": 0.15, "effect": "float",     "color": "#808000", "instrument": "celesta"},

    # ==================== 地点/建筑/空间（20+）====================
    "远方":     {"valence_adj":  0.00, "arousal": 0.30, "effect": "drift",     "color": "#B0C4DE", "instrument": "synth_pad"},
    "故乡":     {"valence_adj":  0.05, "arousal": 0.25, "effect": "glow",      "color": "#DAA520", "instrument": "guitar"},
    "家":       {"valence_adj":  0.15, "arousal": 0.20, "effect": "glow",      "color": "#FFE4B5", "instrument": "piano"},
    "家乡":     {"valence_adj":  0.08, "arousal": 0.25, "effect": "glow",      "color": "#D4A520", "instrument": "guitar"},
    "路":       {"valence_adj":  0.00, "arousal": 0.30, "effect": "drift",     "color": "#808080", "instrument": "piano"},
    "小路":     {"valence_adj":  0.05, "arousal": 0.22, "effect": "drift",     "color": "#909060", "instrument": "guitar"},
    "长路":     {"valence_adj": -0.03, "arousal": 0.32, "effect": "drift",     "color": "#787878", "instrument": "strings"},
    "桥":       {"valence_adj":  0.05, "arousal": 0.22, "effect": "float",     "color": "#A0A0A0", "instrument": "piano"},
    "窗":       {"valence_adj":  0.00, "arousal": 0.15, "effect": "glow",      "color": "#FAFAD2", "instrument": "piano"},
    "窗外":     {"valence_adj": -0.03, "arousal": 0.18, "effect": "glow",      "color": "#E8E8C0", "instrument": "piano"},
    "门":       {"valence_adj":  0.00, "arousal": 0.20, "effect": "float",     "color": "#8B7355", "instrument": "piano"},
    "城市":     {"valence_adj":  0.05, "arousal": 0.50, "effect": "sparkle",   "color": "#FFD700", "instrument": "synth_pad"},
    "街":       {"valence_adj":  0.00, "arousal": 0.40, "effect": "drift",     "color": "#A0A0A0", "instrument": "piano"},
    "街道":     {"valence_adj":  0.00, "arousal": 0.40, "effect": "drift",     "color": "#A0A0A0", "instrument": "piano"},
    "古镇":     {"valence_adj":  0.08, "arousal": 0.20, "effect": "glow",      "color": "#8B7355", "instrument": "guitar"},
    "村庄":     {"valence_adj":  0.10, "arousal": 0.18, "effect": "smoke",     "color": "#A09060", "instrument": "guitar"},
    "花园":     {"valence_adj":  0.20, "arousal": 0.30, "effect": "petals",    "color": "#90EE90", "instrument": "flute"},
    "庭院":     {"valence_adj":  0.12, "arousal": 0.18, "effect": "float",     "color": "#8FAF6F", "instrument": "guitar"},
    "寺庙":     {"valence_adj":  0.05, "arousal": 0.15, "effect": "smoke",     "color": "#8B7355", "instrument": "bells"},
    "教堂":     {"valence_adj":  0.08, "arousal": 0.20, "effect": "glow",      "color": "#B8A888", "instrument": "bells"},
    "废墟":     {"valence_adj": -0.15, "arousal": 0.25, "effect": "dust",      "color": "#6A6A5A", "instrument": "strings"},

    # ==================== 人体/感知/抽象物（15+）====================
    "泪":       {"valence_adj": -0.15, "arousal": 0.40, "effect": "light_rain","color": "#6A8AB0", "instrument": "piano"},
    "眼泪":     {"valence_adj": -0.15, "arousal": 0.40, "effect": "light_rain","color": "#6A8AB0", "instrument": "piano"},
    "笑容":     {"valence_adj":  0.25, "arousal": 0.45, "effect": "sparkle",   "color": "#FFD700", "instrument": "bells"},
    "微笑":     {"valence_adj":  0.22, "arousal": 0.35, "effect": "sparkle",   "color": "#FFD700", "instrument": "bells"},
    "呼吸":     {"valence_adj":  0.00, "arousal": 0.15, "effect": "breeze",    "color": "#C8D8E8", "instrument": "synth_pad"},
    "心跳":     {"valence_adj":  0.05, "arousal": 0.60, "effect": "glow",      "color": "#FF4444", "instrument": "drums"},
    "梦":       {"valence_adj":  0.05, "arousal": 0.15, "effect": "aurora",    "color": "#9370DB", "instrument": "synth_pad"},
    "梦境":     {"valence_adj":  0.08, "arousal": 0.18, "effect": "aurora",    "color": "#8A60D0", "instrument": "synth_pad"},
    "回忆":     {"valence_adj": -0.03, "arousal": 0.20, "effect": "fog",       "color": "#B8A8C8", "instrument": "piano"},
    "记忆":     {"valence_adj": -0.03, "arousal": 0.20, "effect": "fog",       "color": "#B0A0C0", "instrument": "piano"},
    "时光":     {"valence_adj":  0.00, "arousal": 0.20, "effect": "drift",     "color": "#C8B8A0", "instrument": "piano"},
    "岁月":     {"valence_adj": -0.03, "arousal": 0.18, "effect": "drift",     "color": "#B8A888", "instrument": "piano"},
    "影":       {"valence_adj": -0.05, "arousal": 0.22, "effect": "darkness",  "color": "#3A3A4A", "instrument": "piano"},
    "声音":     {"valence_adj":  0.00, "arousal": 0.30, "effect": "ripple",    "color": "#A0B8D0", "instrument": "piano"},
    "歌":       {"valence_adj":  0.10, "arousal": 0.35, "effect": "float",     "color": "#DDA0DD", "instrument": "bells"},
    "歌声":     {"valence_adj":  0.12, "arousal": 0.38, "effect": "float",     "color": "#D890D8", "instrument": "bells"},
    "琴声":     {"valence_adj":  0.10, "arousal": 0.25, "effect": "ripple",    "color": "#C8A860", "instrument": "piano"},
    "钟声":     {"valence_adj":  0.05, "arousal": 0.30, "effect": "ripple",    "color": "#B0A060", "instrument": "bells"},
}

# ==================== 情绪关键词修正表（80+）====================
EMOTION_KEYWORDS = {
    # 强积极
    "幸福":  0.20, "狂喜":  0.25, "兴奋":  0.20, "激动":  0.18, "欣喜":  0.20,
    "陶醉":  0.18, "快乐":  0.15, "开心":  0.15, "高兴":  0.15, "喜悦":  0.15,
    "欢笑":  0.15, "欢乐":  0.15, "欢喜":  0.15, "愉快":  0.12, "惊喜":  0.15,

    # 中积极
    "温暖":  0.12, "温柔":  0.10, "温馨":  0.12, "甜蜜":  0.15, "美好":  0.12,
    "希望":  0.12, "期待":  0.10, "憧憬":  0.10, "向往":  0.08, "自由":  0.10,
    "勇敢":  0.10, "坚强":  0.08, "安心":  0.10, "安宁":  0.08, "满足":  0.10,
    "感恩":  0.10, "珍惜":  0.08, "祝福":  0.12,

    # 弱积极
    "笑":    0.10, "爱":    0.15, "拥抱":  0.12, "亲吻":  0.12, "陪伴":  0.10,
    "喜欢":  0.10, "热爱":  0.12, "眷恋":  0.08, "心动":  0.10, "平静":  0.05,
    "宁静":  0.05, "淡然":  0.03, "释然":  0.08, "轻松":  0.08,

    # 弱消极
    "思念": -0.05, "想念": -0.05, "牵挂": -0.05, "惆怅": -0.08, "感伤": -0.08,
    "沉默": -0.05, "无奈": -0.08, "叹息": -0.08, "遗憾": -0.10, "迷茫": -0.10,
    "彷徨": -0.10, "犹豫": -0.05, "疲惫": -0.10, "倦怠": -0.08,

    # 中消极
    "悲伤": -0.15, "难过": -0.15, "伤心": -0.15, "心痛": -0.15, "哭":   -0.15,
    "流泪": -0.12, "孤独": -0.15, "寂寞": -0.15, "落寞": -0.12, "凄凉": -0.15,
    "失落": -0.15, "离别": -0.10, "分离": -0.10, "告别": -0.08, "冷":   -0.10,
    "寒冷": -0.10, "冰冷": -0.12, "荒凉": -0.12,

    # 强消极
    "绝望": -0.25, "痛苦": -0.20, "崩溃": -0.22, "窒息": -0.20, "恐惧": -0.20,
    "愤怒": -0.15, "暴怒": -0.20, "仇恨": -0.20, "嫉妒": -0.10, "焦虑": -0.12,
    "压抑": -0.15, "煎熬": -0.18, "挣扎": -0.12, "沉沦": -0.18,
}
```

---

## 九、API方案详细说明

### 9.1 推荐API服务商（均有免费额度）

| 服务商 | 免费额度 | 注册地址 | 推荐模型 | 接口格式 |
|--------|----------|----------|----------|----------|
| **DeepSeek** | 注册送500万token | platform.deepseek.com | deepseek-chat | OpenAI兼容 |
| **智谱GLM** | GLM-4-Flash完全免费 | open.bigmodel.cn | glm-4-flash | OpenAI兼容 |
| **通义千问** | 有免费额度 | dashscope.console.aliyun.com | qwen-turbo | OpenAI兼容 |
| **Moonshot** | 注册送token | platform.moonshot.cn | moonshot-v1-8k | OpenAI兼容 |

### 9.2 切换为API方案

只需修改 `config.py` 中两处：

```python
# 1. 切换模式
NLP_MODE = "api"

# 2. 填入你的API密钥（选择任一服务商）
API_KEY = "sk-你的密钥"
```

### 9.3 API方案的优势

- 无需维护词典，AI自动理解任意文字的情感和意象
- 能理解隐喻、反讽、上下文等复杂语义
- 效果更精准，尤其对于非常规输入

### 9.4 API方案的注意事项

- 需要网络连接
- 每次分析有约0.5~2秒的API延迟
- 代码中已实现自动降级：API失败时自动回退到本地方案

---

## 十、8人分工方案

| 编号 | 角色 | 负责内容 | 文件 |
|:---:|------|----------|------|
| P1 | NLP工程师 | 后端分析器 + API对接 | `analyzer.py`、`api_analyzer.py`、`emotion_model.py` |
| P2 | 词典设计师 | 意象词典扩展与调优 | `imagery_dict.py` |
| P3 | 音乐引擎主程 | 音阶/和弦/旋律生成 | `music-engine.js` |
| P4 | 音色设计师 | Tone.js合成器音色调试 | `music-engine.js`（音色部分） |
| P5 | 视觉引擎主程 | 粒子系统 + 渲染循环 | `particle.js`、`visual-engine.js` |
| P6 | 特效设计师 | 高级视觉特效 | `effects.js`、特效预设参数 |
| P7 | 前端工程师 | 页面/样式/WebSocket/交互 | `index.html`、`style.css`、`socket.js`、`main.js` |
| P8 | 项目整合 | 联调/测试/报告/演示 | 全部 + 报告文档 |

---

## 十一、演示方案

### 11.1 现场演示脚本（约5分钟）

**开场（30秒）：** 黑屏，只有一个输入框。讲述："每个人的文字里，都藏着一首歌。"

**第一段 —— 忧伤（1分钟）：**
- "深夜，窗外下着雨"
- "一个人坐在窗边"
- "想起了很远的故乡"

→ 蓝色调，粒子雨，钢琴小调

**第二段 —— 转折（1分钟）：**
- "突然一道闪电划过天空"
- "雨停了"
- "天边出现了一道彩虹"

→ 闪电粒子 → 雨停 → 彩色粒子，调性转亮

**第三段 —— 欢快（1分钟）：**
- "阳光照在花园里"
- "蝴蝶在花间飞舞"
- "春天来了！"

→ 金色暖调，花瓣粒子，明快大调

**第四段 —— 观众互动（1分钟）：** 邀请观众说一句话，现场输入。

**结尾（30秒）：** 点击回放，重播整段情绪旅程。

## 十二、前端完整代码

---

### 12.1 主页面 `templates/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>共感电台 · Emotion Radio</title>
    <link rel="stylesheet" href="/static/css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/tone/14.8.49/Tone.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.4/socket.io.min.js"></script>
</head>
<body>
    <!-- 全屏视觉画布 -->
    <canvas id="visual-canvas"></canvas>

    <!-- 标题 -->
    <div id="title-area">
        <h1>共感电台</h1>
        <p>输入文字，听见你的情绪</p>
    </div>

    <!-- 情绪指示器 -->
    <div id="emotion-indicator">
        <div class="indicator-item">
            <span class="label">情感</span>
            <div class="bar-container">
                <div id="valence-bar" class="bar valence"></div>
            </div>
            <span id="valence-text" class="value">-</span>
        </div>
        <div class="indicator-item">
            <span class="label">能量</span>
            <div class="bar-container">
                <div id="arousal-bar" class="bar arousal"></div>
            </div>
            <span id="arousal-text" class="value">-</span>
        </div>
    </div>

    <!-- 意象标签 -->
    <div id="imagery-tags"></div>

    <!-- 文字显示区 -->
    <div id="text-display"></div>

    <!-- 输入区 -->
    <div id="input-area">
        <input type="text" id="text-input"
               placeholder="输入任意文字，按回车发送..."
               autocomplete="off" />
        <button id="send-btn">✦ 发送</button>
    </div>

    <!-- 时间线 -->
    <div id="timeline-bar">
        <button id="playback-btn">▶ 回放</button>
        <div id="timeline-track"><div id="timeline-progress"></div></div>
        <span id="entry-count">0 条</span>
    </div>

    <script src="/static/js/utils.js"></script>
    <script src="/static/js/socket.js"></script>
    <script src="/static/js/particle.js"></script>
    <script src="/static/js/effects.js"></script>
    <script src="/static/js/visual-engine.js"></script>
    <script src="/static/js/music-engine.js"></script>
    <script src="/static/js/timeline.js"></script>
    <script src="/static/js/main.js"></script>
</body>
</html>
```

---

### 12.2 样式表 `static/css/style.css`

```css
/* ===== 全局 ===== */
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    background: #050510;
    color: #fff;
    font-family: 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif;
    overflow: hidden;
    height: 100vh;
    width: 100vw;
    cursor: default;
}

/* ===== 全屏画布 ===== */
#visual-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: 0;
}

/* ===== 标题（首次显示，输入后淡出）===== */
#title-area {
    position: fixed;
    top: 35%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    z-index: 5;
    transition: opacity 2s ease;
    pointer-events: none;
}

#title-area h1 {
    font-size: 48px;
    font-weight: 200;
    letter-spacing: 12px;
    background: linear-gradient(135deg, rgba(255,255,255,0.8), rgba(255,255,255,0.3));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

#title-area p {
    font-size: 14px;
    color: rgba(255,255,255,0.3);
    letter-spacing: 4px;
    margin-top: 12px;
}

#title-area.hidden {
    opacity: 0;
}

/* ===== 情绪指示器 ===== */
#emotion-indicator {
    position: fixed;
    top: 24px; left: 24px;
    z-index: 10;
    display: flex;
    flex-direction: column;
    gap: 10px;
    opacity: 0;
    transition: opacity 1s ease;
}

#emotion-indicator.visible { opacity: 0.85; }

.indicator-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 12px;
}

.indicator-item .label {
    width: 28px;
    text-align: right;
    color: rgba(255,255,255,0.5);
    font-size: 11px;
    letter-spacing: 2px;
}

.bar-container {
    width: 120px;
    height: 3px;
    background: rgba(255,255,255,0.08);
    border-radius: 2px;
    overflow: hidden;
}

.bar {
    height: 100%;
    width: 0%;
    border-radius: 2px;
    transition: width 1.5s cubic-bezier(0.23, 1, 0.32, 1), background 1.5s ease;
}

.bar.valence { background: linear-gradient(90deg, #4a6cf7, #7c4dff); }
.bar.arousal { background: linear-gradient(90deg, #f7524a, #ff6b6b); }

.indicator-item .value {
    width: 32px;
    font-size: 11px;
    color: rgba(255,255,255,0.4);
    font-family: 'SF Mono', 'Fira Code', monospace;
}

/* ===== 意象标签 ===== */
#imagery-tags {
    position: fixed;
    top: 76px; left: 24px;
    z-index: 10;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    max-width: 320px;
}

#imagery-tags .tag {
    padding: 3px 12px;
    border-radius: 20px;
    font-size: 11px;
    letter-spacing: 1px;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.08);
    backdrop-filter: blur(8px);
    animation: tagIn 0.6s cubic-bezier(0.23, 1, 0.32, 1) forwards;
    opacity: 0;
    transform: scale(0.8);
}

@keyframes tagIn {
    to { opacity: 1; transform: scale(1); }
}

/* ===== 文字显示区 ===== */
#text-display {
    position: fixed;
    top: 28%;
    left: 50%;
    transform: translateX(-50%);
    z-index: 5;
    text-align: center;
    max-width: 75%;
    pointer-events: none;
}

#text-display .text-line {
    font-size: 30px;
    font-weight: 200;
    letter-spacing: 3px;
    line-height: 1.6;
    text-shadow: 0 0 40px rgba(255,255,255,0.2), 0 0 80px rgba(255,255,255,0.05);
    animation: textSlideIn 1.2s cubic-bezier(0.23, 1, 0.32, 1) forwards;
    margin-bottom: 16px;
    opacity: 0;
}

@keyframes textSlideIn {
    0%   { opacity: 0; transform: translateY(30px) scale(0.97); filter: blur(4px); }
    100% { opacity: 1; transform: translateY(0) scale(1); filter: blur(0); }
}

@keyframes textFadeOut {
    to { opacity: 0.15; transform: scale(0.98); filter: blur(1px); }
}

/* ===== 输入区 ===== */
#input-area {
    position: fixed;
    bottom: 56px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 20;
    display: flex;
    gap: 10px;
    width: min(640px, 88vw);
}

#text-input {
    flex: 1;
    padding: 14px 24px;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 32px;
    background: rgba(255,255,255,0.04);
    backdrop-filter: blur(20px);
    color: #fff;
    font-size: 16px;
    outline: none;
    transition: border-color 0.4s, box-shadow 0.4s;
    letter-spacing: 1px;
}

#text-input:focus {
    border-color: rgba(255,255,255,0.25);
    box-shadow: 0 0 30px rgba(255,255,255,0.03);
}

#text-input::placeholder {
    color: rgba(255,255,255,0.2);
    letter-spacing: 1px;
}

#send-btn {
    padding: 14px 28px;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 32px;
    background: rgba(255,255,255,0.06);
    backdrop-filter: blur(20px);
    color: rgba(255,255,255,0.7);
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s ease;
    letter-spacing: 2px;
    white-space: nowrap;
}

#send-btn:hover {
    background: rgba(255,255,255,0.12);
    color: #fff;
    border-color: rgba(255,255,255,0.2);
}

/* ===== 时间线 ===== */
#timeline-bar {
    position: fixed;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 20;
    display: flex;
    align-items: center;
    gap: 10px;
    width: min(500px, 75vw);
    opacity: 0;
    transition: opacity 1s ease;
}

#timeline-bar.visible { opacity: 0.5; }

#playback-btn {
    padding: 4px 14px;
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 16px;
    background: none;
    color: rgba(255,255,255,0.6);
    font-size: 12px;
    cursor: pointer;
    white-space: nowrap;
    transition: all 0.3s;
}

#playback-btn:hover { border-color: rgba(255,255,255,0.3); color: #fff; }

#timeline-track {
    flex: 1;
    height: 2px;
    background: rgba(255,255,255,0.08);
    border-radius: 1px;
    overflow: hidden;
}

#timeline-progress {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, rgba(255,255,255,0.2), rgba(255,255,255,0.5));
    transition: width 0.5s ease;
}

#entry-count {
    font-size: 11px;
    color: rgba(255,255,255,0.3);
    font-family: monospace;
    white-space: nowrap;
}
```

---

### 12.3 工具函数 `static/js/utils.js`

```javascript
function lerp(a, b, t) { return a + (b - a) * t; }
function clamp(v, min, max) { return Math.min(max, Math.max(min, v)); }
function randomRange(min, max) { return min + Math.random() * (max - min); }
function randomChoice(arr) { return arr[Math.floor(Math.random() * arr.length)]; }
function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

// HSL → CSS字符串
function hsl(h, s, l, a) {
    return a !== undefined ? `hsla(${h},${s}%,${l}%,${a})` : `hsl(${h},${s}%,${l}%)`;
}

// Hex → {r, g, b}
function hexToRgb(hex) {
    hex = hex.replace('#', '');
    return {
        r: parseInt(hex.substring(0, 2), 16),
        g: parseInt(hex.substring(2, 4), 16),
        b: parseInt(hex.substring(4, 6), 16),
    };
}
```

---

### 12.4 WebSocket通信 `static/js/socket.js`

```javascript
const Socket = (() => {
    let socket = null;
    let onResultCallback = null;

    function connect() {
        socket = io();
        socket.on('connect', () => console.log('✅ 已连接'));
        socket.on('disconnect', () => console.log('❌ 断开'));
        socket.on('emotion_result', (data) => {
            if (onResultCallback) onResultCallback(data);
        });
    }

    function sendText(text) {
        if (socket && socket.connected) {
            socket.emit('analyze_text', { text });
        }
    }

    function onResult(cb) { onResultCallback = cb; }

    return { connect, sendText, onResult };
})();
```

---

### 12.5 粒子系统 `static/js/particle.js`（增强版）

```javascript
/**
 * 增强版粒子类 —— 支持拖尾、发光、多形态
 */
class Particle {
    constructor(x, y, config) {
        this.x = x;
        this.y = y;
        this.vx = config.vx + (Math.random() - 0.5) * (config.randomness || 1);
        this.vy = config.vy + (Math.random() - 0.5) * (config.randomness || 1);
        this.life = config.life || 100;
        this.maxLife = this.life;
        this.color = config.color || '#ffffff';
        this.size = config.size || 3;
        this.shape = config.shape || 'circle';
        this.gravity = config.gravity || 0;
        this.friction = config.friction || 1;
        this.opacity = 1;
        this.rotation = Math.random() * Math.PI * 2;
        this.rotationSpeed = (Math.random() - 0.5) * 0.05;
        this.trail = config.trail || false;     // 是否有拖尾
        this.glowSize = config.glowSize || 0;   // 发光半径倍数
        this.history = [];                       // 拖尾位置历史
    }

    update() {
        // 拖尾记录
        if (this.trail && this.life % 2 === 0) {
            this.history.push({ x: this.x, y: this.y });
            if (this.history.length > 8) this.history.shift();
        }

        this.vy += this.gravity;
        this.vx *= this.friction;
        this.vy *= this.friction;
        this.x += this.vx;
        this.y += this.vy;
        this.life--;
        this.opacity = Math.max(0, this.life / this.maxLife);
        this.rotation += this.rotationSpeed;
    }

    draw(ctx) {
        ctx.save();
        ctx.globalAlpha = this.opacity;

        const rgb = hexToRgb(this.color);

        // 拖尾绘制
        if (this.trail && this.history.length > 1) {
            ctx.beginPath();
            ctx.moveTo(this.history[0].x, this.history[0].y);
            for (let i = 1; i < this.history.length; i++) {
                ctx.lineTo(this.history[i].x, this.history[i].y);
            }
            ctx.lineTo(this.x, this.y);
            ctx.strokeStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity * 0.3})`;
            ctx.lineWidth = this.size * 0.5;
            ctx.stroke();
        }

        // 外发光
        if (this.glowSize > 0) {
            const gradient = ctx.createRadialGradient(
                this.x, this.y, 0,
                this.x, this.y, this.size * this.glowSize
            );
            gradient.addColorStop(0, `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity * 0.4})`);
            gradient.addColorStop(1, `rgba(${rgb.r},${rgb.g},${rgb.b},0)`);
            ctx.fillStyle = gradient;
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.size * this.glowSize, 0, Math.PI * 2);
            ctx.fill();
        }

        // 主形态
        ctx.fillStyle = this.color;
        ctx.translate(this.x, this.y);
        ctx.rotate(this.rotation);

        switch (this.shape) {
            case 'circle':
                ctx.beginPath();
                ctx.arc(0, 0, this.size, 0, Math.PI * 2);
                ctx.fill();
                break;

            case 'raindrop':
                ctx.beginPath();
                ctx.ellipse(0, 0, this.size * 0.4, this.size * 2.5, 0, 0, Math.PI * 2);
                ctx.fill();
                break;

            case 'snowflake':
                ctx.fillStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity})`;
                ctx.beginPath();
                ctx.arc(0, 0, this.size, 0, Math.PI * 2);
                ctx.fill();
                ctx.strokeStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity * 0.6})`;
                ctx.lineWidth = 0.5;
                for (let i = 0; i < 6; i++) {
                    const angle = (Math.PI / 3) * i;
                    ctx.beginPath();
                    ctx.moveTo(0, 0);
                    ctx.lineTo(Math.cos(angle) * this.size * 2.5, Math.sin(angle) * this.size * 2.5);
                    ctx.stroke();
                }
                break;

            case 'petal':
                ctx.beginPath();
                ctx.moveTo(0, -this.size * 1.5);
                ctx.bezierCurveTo(
                    this.size * 1.2, -this.size * 0.5,
                    this.size * 1.2,  this.size * 0.5,
                    0, this.size * 1.5
                );
                ctx.bezierCurveTo(
                    -this.size * 1.2,  this.size * 0.5,
                    -this.size * 1.2, -this.size * 0.5,
                    0, -this.size * 1.5
                );
                ctx.fill();
                break;

            case 'leaf':
                ctx.beginPath();
                ctx.moveTo(0, -this.size * 2);
                ctx.bezierCurveTo(
                    this.size * 1.5, -this.size,
                    this.size * 1.5,  this.size,
                    0, this.size * 2
                );
                ctx.bezierCurveTo(
                    -this.size * 1.5,  this.size,
                    -this.size * 1.5, -this.size,
                    0, -this.size * 2
                );
                ctx.fill();
                // 叶脉
                ctx.strokeStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity * 0.3})`;
                ctx.lineWidth = 0.5;
                ctx.beginPath();
                ctx.moveTo(0, -this.size * 1.8);
                ctx.lineTo(0, this.size * 1.8);
                ctx.stroke();
                break;

            case 'star':
                this._drawStar(ctx, 5, this.size, this.size * 0.4);
                break;

            case 'diamond':
                ctx.beginPath();
                ctx.moveTo(0, -this.size * 1.5);
                ctx.lineTo(this.size, 0);
                ctx.lineTo(0, this.size * 1.5);
                ctx.lineTo(-this.size, 0);
                ctx.closePath();
                ctx.fill();
                break;

            case 'spark':
                // 核心点
                ctx.beginPath();
                ctx.arc(0, 0, this.size * 0.6, 0, Math.PI * 2);
                ctx.fill();
                // 十字光芒
                ctx.strokeStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${this.opacity * 0.5})`;
                ctx.lineWidth = this.size * 0.3;
                ctx.beginPath();
                ctx.moveTo(0, -this.size * 2);
                ctx.lineTo(0, this.size * 2);
                ctx.moveTo(-this.size * 2, 0);
                ctx.lineTo(this.size * 2, 0);
                ctx.stroke();
                break;

            default:
                ctx.beginPath();
                ctx.arc(0, 0, this.size, 0, Math.PI * 2);
                ctx.fill();
        }

        ctx.restore();
    }

    _drawStar(ctx, spikes, outerR, innerR) {
        let rot = -Math.PI / 2;
        const step = Math.PI / spikes;
        ctx.beginPath();
        for (let i = 0; i < spikes; i++) {
            ctx.lineTo(Math.cos(rot) * outerR, Math.sin(rot) * outerR);
            rot += step;
            ctx.lineTo(Math.cos(rot) * innerR, Math.sin(rot) * innerR);
            rot += step;
        }
        ctx.closePath();
        ctx.fill();
    }

    isDead() { return this.life <= 0; }
}


/* ========== 粒子效果预设（增强版）========== */
const EFFECT_PRESETS = {
    // --- 雨系 ---
    rain:       { shape: 'raindrop', spawnArea: 'top', vy: 9, vx: -1.5, color: '#6688cc', size: 2, life: 80, rate: 5, randomness: 0.5, gravity: 0.08, trail: true, glowSize: 0 },
    heavy_rain: { shape: 'raindrop', spawnArea: 'top', vy: 14, vx: -3, color: '#4466aa', size: 2.5, life: 50, rate: 14, randomness: 1, gravity: 0.12, trail: true, glowSize: 0 },
    light_rain: { shape: 'raindrop', spawnArea: 'top', vy: 4, vx: -0.5, color: '#88aadd', size: 1.5, life: 130, rate: 2, randomness: 0.3, gravity: 0.03, trail: false, glowSize: 0 },

    // --- 雪系 ---
    snow:       { shape: 'snowflake', spawnArea: 'top', vy: 0.8, vx: 0, color: '#e8e8ff', size: 2.5, life: 250, rate: 1.8, randomness: 1.5, gravity: 0, glowSize: 3 },
    heavy_snow: { shape: 'snowflake', spawnArea: 'top', vy: 1.2, vx: 0, color: '#d0d0f0', size: 3.5, life: 200, rate: 4, randomness: 2, gravity: 0, glowSize: 2 },

    // --- 风系 ---
    wind:       { shape: 'circle', spawnArea: 'left', vy: 0, vx: 7, color: '#a8c8c8', size: 1.2, life: 60, rate: 4, randomness: 2, gravity: 0, trail: true, glowSize: 0 },
    breeze:     { shape: 'circle', spawnArea: 'left', vy: 0, vx: 2.5, color: '#c8e8d8', size: 1, life: 110, rate: 1.2, randomness: 1.2, gravity: 0, trail: false, glowSize: 2 },
    storm:      { shape: 'raindrop', spawnArea: 'top', vy: 16, vx: -6, color: '#3344aa', size: 2, life: 35, rate: 18, randomness: 4, gravity: 0.2, trail: true, glowSize: 0 },

    // --- 火系 ---
    fire:       { shape: 'circle', spawnArea: 'bottom', vy: -4, vx: 0, color: '#ff4400', size: 4, life: 55, rate: 7, randomness: 2.5, gravity: -0.06, glowSize: 5, multiColor: ['#ff2200','#ff4400','#ff6600','#ffaa00','#ffcc00'] },
    firework:   { shape: 'spark', spawnArea: 'center_burst', vy: 0, vx: 0, color: '#ff69b4', size: 3, life: 50, rate: 0, randomness: 8, gravity: 0.03, trail: true, glowSize: 4, multiColor: ['#ff0066','#ff69b4','#ffaa00','#00ffcc','#6644ff','#ff4444'] },
    candle:     { shape: 'circle', spawnArea: 'center', vy: -1.8, vx: 0, color: '#ffd700', size: 3, life: 40, rate: 2.5, randomness: 0.6, gravity: -0.03, glowSize: 6 },

    // --- 闪电 ---
    lightning:  { shape: 'spark', spawnArea: 'top', vy: 20, vx: 0, color: '#ffffff', size: 2, life: 8, rate: 0.15, randomness: 15, gravity: 0, glowSize: 10, multiColor: ['#ffffff','#eeeeff','#aaaaff'] },

    // --- 花与叶 ---
    petals:     { shape: 'petal', spawnArea: 'top', vy: 1.2, vx: 1.5, color: '#ffb6c1', size: 4, life: 180, rate: 1.5, randomness: 2, gravity: 0.005, glowSize: 0, multiColor: ['#ffb6c1','#ffb7c5','#ffc0cb','#ff99aa','#ffddee'] },
    leaves:     { shape: 'leaf', spawnArea: 'top', vy: 0.8, vx: 1, color: '#d2691e', size: 4.5, life: 200, rate: 1, randomness: 2, gravity: 0.008, glowSize: 0, multiColor: ['#d2691e','#daa520','#b8860b','#cd853f','#8b4513'] },

    // --- 星/光/闪 ---
    stars:      { shape: 'star', spawnArea: 'random', vy: 0, vx: 0, color: '#ffd700', size: 2, life: 150, rate: 0.6, randomness: 0, gravity: 0, glowSize: 6 },
    sparkle:    { shape: 'diamond', spawnArea: 'random', vy: -0.3, vx: 0, color: '#ffd700', size: 3, life: 40, rate: 4, randomness: 3, gravity: 0, glowSize: 5, multiColor: ['#ffd700','#fffacd','#ffffff'] },
    glow:       { shape: 'circle', spawnArea: 'center', vy: -0.4, vx: 0, color: '#fffacd', size: 5, life: 120, rate: 1, randomness: 2.5, gravity: 0, glowSize: 8 },
    burst:      { shape: 'spark', spawnArea: 'center', vy: 0, vx: 0, color: '#ffd700', size: 3.5, life: 45, rate: 8, randomness: 6, gravity: 0, trail: true, glowSize: 5 },
    shooting_star: { shape: 'circle', spawnArea: 'top_right', vy: 4, vx: -6, color: '#fffacd', size: 2, life: 40, rate: 0.1, randomness: 1, gravity: 0, trail: true, glowSize: 6 },

    // --- 水系 ---
    wave:       { shape: 'circle', spawnArea: 'bottom_wave', vy: -1.2, vx: 0, color: '#1e90ff', size: 3, life: 70, rate: 3, randomness: 1.5, gravity: 0.015, glowSize: 3 },
    ripple:     { shape: 'circle', spawnArea: 'center', vy: 0, vx: 0, color: '#5f9ea0', size: 2, life: 90, rate: 2, randomness: 4, gravity: 0, glowSize: 4 },
    flow:       { shape: 'circle', spawnArea: 'left', vy: 0, vx: 2.5, color: '#5b9bd5', size: 2.5, life: 130, rate: 2, randomness: 1.5, gravity: 0.008, glowSize: 2 },

    // --- 天象 ---
    sunlight:   { shape: 'diamond', spawnArea: 'top', vy: 2, vx: 0, color: '#ffd700', size: 2.5, life: 90, rate: 2, randomness: 2, gravity: 0.01, glowSize: 6, multiColor: ['#ffd700','#ffcc00','#ffe44d'] },
    sunrise:    { shape: 'circle', spawnArea: 'bottom_center', vy: -1.5, vx: 0, color: '#ff4500', size: 4, life: 100, rate: 3, randomness: 3, gravity: 0, glowSize: 6, multiColor: ['#ff4500','#ff6347','#ffa500','#ffd700'] },
    sunset:     { shape: 'circle', spawnArea: 'bottom', vy: -0.6, vx: 0, color: '#ff6347', size: 3.5, life: 130, rate: 2, randomness: 2, gravity: 0, glowSize: 5, multiColor: ['#ff6347','#cd853f','#daa520','#9370db'] },
    moonlight:  { shape: 'circle', spawnArea: 'top_center', vy: 0.4, vx: 0, color: '#fffacd', size: 2, life: 180, rate: 0.8, randomness: 2.5, gravity: 0, glowSize: 8 },
    rainbow:    { shape: 'diamond', spawnArea: 'top', vy: 0.4, vx: 0, color: '#ff69b4', size: 3, life: 120, rate: 3, randomness: 4, glowSize: 4, multiColor: ['#ff4444','#ff8800','#ffdd00','#44ff44','#4488ff','#8844ff','#ff44ff'] },
    clouds:     { shape: 'circle', spawnArea: 'top', vy: 0, vx: 0.3, color: '#d3d3e8', size: 25, life: 350, rate: 0.15, randomness: 0.2, gravity: 0, glowSize: 2 },

    // --- 烟/雾/尘 ---
    fog:        { shape: 'circle', spawnArea: 'random', vy: 0, vx: 0.25, color: '#b8b8c8', size: 20, life: 300, rate: 0.25, randomness: 0.3, gravity: 0, glowSize: 3 },
    smoke:      { shape: 'circle', spawnArea: 'bottom', vy: -0.8, vx: 0, color: '#888899', size: 12, life: 200, rate: 0.5, randomness: 1, gravity: -0.005, glowSize: 3 },
    dust:       { shape: 'circle', spawnArea: 'random', vy: 0.1, vx: 0.6, color: '#edc9af', size: 1.5, life: 220, rate: 1, randomness: 0.5, gravity: 0, glowSize: 2 },

    // --- 暗系 ---
    darkness:   { shape: 'circle', spawnArea: 'random', vy: 0.08, vx: 0, color: '#1a1a3e', size: 12, life: 250, rate: 0.3, randomness: 0.5, gravity: 0, glowSize: 4 },

    // --- 特殊效果 ---
    aurora:     { shape: 'circle', spawnArea: 'top', vy: 0.3, vx: 0, color: '#00ff88', size: 6, life: 200, rate: 1, randomness: 3, gravity: 0, glowSize: 10, multiColor: ['#00ff88','#00ccff','#8844ff','#ff44ff','#00ffcc'] },
    firefly:    { shape: 'circle', spawnArea: 'random', vy: 0, vx: 0, color: '#ccff00', size: 2, life: 120, rate: 0.5, randomness: 0.3, gravity: 0, glowSize: 8 },
    drift:      { shape: 'circle', spawnArea: 'random', vy: 0.15, vx: 0.25, color: '#8888aa', size: 2, life: 220, rate: 0.8, randomness: 0.5, gravity: 0, glowSize: 3 },
    float:      { shape: 'circle', spawnArea: 'random', vy: -0.25, vx: 0, color: '#ffffff', size: 2, life: 180, rate: 1, randomness: 0.6, gravity: 0, glowSize: 4 },
};
```

---

### 12.6 高级视觉特效 `static/js/effects.js`

```javascript
/**
 * 高级视觉特效模块
 * 提供背景渐变动画、全局辉光(bloom)、波纹、呼吸灯等效果
 */
const Effects = (() => {
    let time = 0;

    /**
     * 绘制背景渐变（比纯色更有层次感）
     */
    function drawBackground(ctx, w, h, bg, opacity) {
        // 多层径向渐变叠加
        const gradient1 = ctx.createRadialGradient(
            w * 0.3, h * 0.3, 0,
            w * 0.3, h * 0.3, w * 0.8
        );
        gradient1.addColorStop(0, hsl(bg.h + 10, bg.s + 10, bg.l + 8, opacity));
        gradient1.addColorStop(1, hsl(bg.h, bg.s, bg.l, opacity));
        ctx.fillStyle = gradient1;
        ctx.fillRect(0, 0, w, h);

        // 第二层：右下方微弱暖光
        const gradient2 = ctx.createRadialGradient(
            w * 0.7, h * 0.7, 0,
            w * 0.7, h * 0.7, w * 0.6
        );
        gradient2.addColorStop(0, hsl(bg.h - 30, bg.s, bg.l + 3, opacity * 0.5));
        gradient2.addColorStop(1, 'rgba(0,0,0,0)');
        ctx.fillStyle = gradient2;
        ctx.fillRect(0, 0, w, h);
    }

    /**
     * 全局辉光/Bloom后处理效果
     */
    function drawBloom(ctx, w, h, intensity) {
        if (intensity < 0.1) return;
        time += 0.005;

        // 中心辉光（缓慢呼吸）
        const breathe = 0.5 + Math.sin(time * 2) * 0.15;
        const radius = w * (0.3 + breathe * 0.2);

        const gradient = ctx.createRadialGradient(
            w / 2, h / 2, 0,
            w / 2, h / 2, radius
        );
        gradient.addColorStop(0, `rgba(255,255,255,${intensity * 0.06 * breathe})`);
        gradient.addColorStop(0.5, `rgba(255,255,255,${intensity * 0.02 * breathe})`);
        gradient.addColorStop(1, 'rgba(255,255,255,0)');
        ctx.fillStyle = gradient;
        ctx.fillRect(0, 0, w, h);
    }

    /**
     * 渐变光晕条（底部/顶部氛围光）
     */
    function drawAmbientGlow(ctx, w, h, palette, intensity) {
        if (intensity < 0.1 || !palette || palette.length === 0) return;

        const rgb = hexToRgb(palette[0]);

        // 底部光晕
        const bottomGlow = ctx.createLinearGradient(0, h, 0, h * 0.7);
        bottomGlow.addColorStop(0, `rgba(${rgb.r},${rgb.g},${rgb.b},${intensity * 0.15})`);
        bottomGlow.addColorStop(1, 'rgba(0,0,0,0)');
        ctx.fillStyle = bottomGlow;
        ctx.fillRect(0, h * 0.7, w, h * 0.3);

        // 顶部微光
        if (palette.length > 1) {
            const rgb2 = hexToRgb(palette[1]);
            const topGlow = ctx.createLinearGradient(0, 0, 0, h * 0.25);
            topGlow.addColorStop(0, `rgba(${rgb2.r},${rgb2.g},${rgb2.b},${intensity * 0.08})`);
            topGlow.addColorStop(1, 'rgba(0,0,0,0)');
            ctx.fillStyle = topGlow;
            ctx.fillRect(0, 0, w, h * 0.25);
        }
    }

    /**
     * 暗角效果（画面边缘压暗，突出中心）
     */
    function drawVignette(ctx, w, h) {
        const gradient = ctx.createRadialGradient(
            w / 2, h / 2, w * 0.25,
            w / 2, h / 2, w * 0.75
        );
        gradient.addColorStop(0, 'rgba(0,0,0,0)');
        gradient.addColorStop(1, 'rgba(0,0,0,0.4)');
        ctx.fillStyle = gradient;
        ctx.fillRect(0, 0, w, h);
    }

    return { drawBackground, drawBloom, drawAmbientGlow, drawVignette };
})();
```

---

### 12.7 视觉引擎 `static/js/visual-engine.js`（增强版）

```javascript
/**
 * 增强版视觉引擎
 * 管理Canvas渲染、背景渐变、粒子生成、后处理效果
 */
const VisualEngine = (() => {
    let canvas, ctx;
    let width, height;
    let particles = [];
    let activeEffects = [];
    let frameCounter = 0;

    // 当前/目标状态（平滑过渡）
    let currentBg = { h: 230, s: 20, l: 8 };
    let targetBg  = { h: 230, s: 20, l: 8 };
    let currentGlow = 0.2;
    let targetGlow = 0.2;
    let currentSpeed = 1.0;
    let targetSpeed = 1.0;
    let currentPalette = ['#555577', '#333355', '#222244'];

    function init() {
        canvas = document.getElementById('visual-canvas');
        ctx = canvas.getContext('2d');
        resize();
        window.addEventListener('resize', resize);
        animate();
    }

    function resize() {
        width = canvas.width = window.innerWidth;
        height = canvas.height = window.innerHeight;
    }

    function updateParams(vp) {
        targetBg = vp.background;
        targetGlow = vp.glow_intensity;
        targetSpeed = vp.particle_speed;
        currentPalette = vp.color_palette;

        activeEffects = vp.particle_effects.map(name => {
            const preset = EFFECT_PRESETS[name];
            if (!preset) return null;
            return { name, ...preset };
        }).filter(Boolean);

        // 烟花效果：立即爆发一次
        if (vp.particle_effects.includes('firework')) {
            burstFirework();
        }
    }

    function burstFirework() {
        const cx = width * (0.3 + Math.random() * 0.4);
        const cy = height * (0.2 + Math.random() * 0.3);
        const colors = ['#ff0066','#ff69b4','#ffaa00','#00ffcc','#6644ff','#ffffff'];
        for (let i = 0; i < 60; i++) {
            const angle = (Math.PI * 2 / 60) * i;
            const speed = 3 + Math.random() * 4;
            particles.push(new Particle(cx, cy, {
                vx: Math.cos(angle) * speed,
                vy: Math.sin(angle) * speed,
                color: randomChoice(colors),
                size: 2 + Math.random() * 2,
                shape: 'spark',
                life: 40 + Math.random() * 30,
                randomness: 0.5,
                gravity: 0.04,
                trail: true,
                glowSize: 5,
            }));
        }
    }

    function animate() {
        requestAnimationFrame(animate);
        frameCounter++;

        // 平滑过渡
        const t = 0.02;
        currentBg.h += (targetBg.h - currentBg.h) * t;
        currentBg.s += (targetBg.s - currentBg.s) * t;
        currentBg.l += (targetBg.l - currentBg.l) * t;
        currentGlow += (targetGlow - currentGlow) * t;
        currentSpeed += (targetSpeed - currentSpeed) * 0.05;

        // === 绘制 ===

        // 1. 背景（渐变叠加，产生拖尾效果）
        Effects.drawBackground(ctx, width, height, currentBg, 0.12);

        // 2. 生成粒子
        for (const effect of activeEffects) {
            const adjustedRate = effect.rate * currentSpeed;
            const count = Math.floor(adjustedRate);
            const rem = adjustedRate - count;
            for (let i = 0; i < count; i++) spawnParticle(effect);
            if (Math.random() < rem) spawnParticle(effect);
        }

        // 3. 更新 & 绘制粒子
        for (let i = particles.length - 1; i >= 0; i--) {
            const p = particles[i];
            p.update();
            p.draw(ctx);
            if (p.isDead() || p.x < -80 || p.x > width + 80 ||
                p.y < -80 || p.y > height + 80) {
                particles.splice(i, 1);
            }
        }

        // 4. 后处理效果
        Effects.drawBloom(ctx, width, height, currentGlow);
        Effects.drawAmbientGlow(ctx, width, height, currentPalette, currentGlow);
        Effects.drawVignette(ctx, width, height);

        // 5. 粒子数量限制
        if (particles.length > 1000) particles.splice(0, 150);
    }

    function spawnParticle(effect) {
        let x, y;
        switch (effect.spawnArea) {
            case 'top':           x = Math.random() * width; y = -15; break;
            case 'top_center':    x = width/2 + (Math.random()-0.5)*width*0.3; y = -15; break;
            case 'top_right':     x = width + 10; y = Math.random() * height * 0.3; break;
            case 'bottom':        x = Math.random() * width; y = height + 15; break;
            case 'bottom_center': x = width/2 + (Math.random()-0.5)*width*0.3; y = height + 15; break;
            case 'bottom_wave':
                x = Math.random() * width;
                y = height - 40 + Math.sin(frameCounter*0.02 + x*0.008)*25;
                break;
            case 'left':          x = -15; y = Math.random() * height; break;
            case 'center':        x = width/2 + (Math.random()-0.5)*250; y = height/2 + (Math.random()-0.5)*250; break;
            case 'random':
            default:              x = Math.random() * width; y = Math.random() * height; break;
        }

        let color = effect.color;
        if (effect.multiColor) color = randomChoice(effect.multiColor);

        particles.push(new Particle(x, y, {
            vx: effect.vx,
            vy: effect.vy,
            color: color,
            size: effect.size + (Math.random()-0.5) * effect.size * 0.6,
            shape: effect.shape,
            life: effect.life + Math.floor((Math.random()-0.5) * effect.life * 0.3),
            randomness: effect.randomness,
            gravity: effect.gravity || 0,
            friction: effect.friction || 1,
            trail: effect.trail || false,
            glowSize: effect.glowSize || 0,
        }));
    }

    return { init, updateParams };
})();
```

---

### 12.8 音乐引擎 `static/js/music-engine.js`

```javascript
const MusicEngine = (() => {
    const SCALES = {
        major:            [0, 2, 4, 5, 7, 9, 11],
        minor:            [0, 2, 3, 5, 7, 8, 10],
        pentatonic_major: [0, 2, 4, 7, 9],
        pentatonic_minor: [0, 3, 5, 7, 10],
    };

    const CHORD_TYPES = {
        major: [0, 4, 7],
        minor: [0, 3, 7],
    };

    const MAJOR_PROGS = [[0,3,4,0],[0,5,3,4],[0,4,5,3]];
    const MINOR_PROGS = [[0,3,4,0],[0,5,2,4],[0,3,5,4]];

    let synths = {};
    let currentParams = null;
    let isPlaying = false;
    let melodyLoop = null, chordLoop = null, bassLoop = null;
    let toneStarted = false;
    let currentScale = [];
    let currentChordProg = [];
    let chordIdx = 0, melodyIdx = 5;

    function init() {
        synths.piano = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'triangle8' },
            envelope: { attack: 0.05, decay: 0.4, sustain: 0.3, release: 1.2 }
        }).toDestination();

        synths.strings = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'fatsawtooth', spread: 20, count: 3 },
            envelope: { attack: 0.4, decay: 0.6, sustain: 0.5, release: 1.8 }
        }).toDestination();

        synths.flute = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'sine' },
            envelope: { attack: 0.15, decay: 0.3, sustain: 0.7, release: 1.0 }
        }).toDestination();

        synths.synth_pad = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'fatsine', spread: 30, count: 3 },
            envelope: { attack: 1.0, decay: 1.5, sustain: 0.6, release: 2.5 }
        }).toDestination();

        synths.guitar = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'triangle' },
            envelope: { attack: 0.02, decay: 0.6, sustain: 0.15, release: 0.9 }
        }).toDestination();

        synths.celesta = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'sine' },
            envelope: { attack: 0.01, decay: 1.0, sustain: 0.05, release: 2.0 }
        }).toDestination();

        synths.bells = new Tone.PolySynth(Tone.Synth, {
            oscillator: { type: 'sine' },
            envelope: { attack: 0.005, decay: 1.2, sustain: 0.03, release: 2.5 }
        }).toDestination();

        synths.drums = new Tone.MembraneSynth({
            envelope: { attack: 0.01, decay: 0.3, sustain: 0.05, release: 0.5 }
        }).toDestination();

        synths.reverb = new Tone.Reverb({ decay: 4, wet: 0.3 }).toDestination();

        Object.keys(synths).forEach(k => {
            if (k !== 'reverb' && k !== 'drums' && synths[k].connect) {
                synths[k].connect(synths.reverb);
            }
        });

        // 降低各合成器音量避免爆音
        Object.keys(synths).forEach(k => {
            if (synths[k].volume) synths[k].volume.value = -8;
        });
    }

    async function ensureStarted() {
        if (!toneStarted) {
            await Tone.start();
            toneStarted = true;
        }
    }

    async function updateParams(mp) {
        await ensureStarted();
        currentParams = mp;
        Tone.Transport.bpm.rampTo(mp.tempo, 2);
        synths.reverb.wet.rampTo(mp.reverb, 2);

        const rootMidi = noteToMidi(mp.root, mp.octave);
        const intervals = SCALES[mp.scale] || SCALES.major;
        currentScale = [];
        for (let oct = -1; oct <= 1; oct++) {
            for (const iv of intervals) currentScale.push(rootMidi + iv + oct * 12);
        }

        const isMin = mp.scale.includes('minor');
        const progs = isMin ? MINOR_PROGS : MAJOR_PROGS;
        currentChordProg = progs[Math.floor(Math.random() * progs.length)];

        if (!isPlaying) { startLoops(); isPlaying = true; }
    }

    function startLoops() {
        chordLoop = new Tone.Loop((time) => {
            if (!currentParams) return;
            const intervals = SCALES[currentParams.scale] || SCALES.major;
            const rootMidi = noteToMidi(currentParams.root, currentParams.octave - 1);
            const degree = currentChordProg[chordIdx % currentChordProg.length];
            const chordRoot = rootMidi + (intervals[degree % intervals.length] || 0);

            const isMin = currentParams.scale.includes('minor');
            const ct = isMin ?
                (degree === 0 || degree === 3 || degree === 4 ? 'minor' : 'major') :
                (degree === 1 || degree === 2 || degree === 5 ? 'minor' : 'major');

            const notes = CHORD_TYPES[ct].map(o => Tone.Frequency(chordRoot + o, 'midi').toNote());
            const inst = synths[currentParams.instrument] || synths.piano;
            inst.triggerAttackRelease(notes, '2n', time, currentParams.velocity * 0.35);
            chordIdx++;
        }, '2n');

        melodyLoop = new Tone.Loop((time) => {
            if (!currentParams || currentScale.length === 0) return;
            if (Math.random() > currentParams.density) return;

            const jump = Math.floor((Math.random() - 0.5) * 4);
            melodyIdx = clamp(melodyIdx + jump, 0, currentScale.length - 1);

            const note = Tone.Frequency(currentScale[melodyIdx], 'midi').toNote();
            const inst = synths[currentParams.instrument] || synths.piano;
            const durs = ['8n', '8n', '4n', '4n', '2n'];
            inst.triggerAttackRelease(note, randomChoice(durs), time, currentParams.velocity);
        }, '8n');

        bassLoop = new Tone.Loop((time) => {
            if (!currentParams) return;
            const intervals = SCALES[currentParams.scale] || SCALES.major;
            const rootMidi = noteToMidi(currentParams.root, 2);
            const degree = currentChordProg[chordIdx % currentChordProg.length];
            const bassNote = rootMidi + (intervals[degree % intervals.length] || 0);
            const note = Tone.Frequency(bassNote, 'midi').toNote();
            synths.synth_pad.triggerAttackRelease(note, '2n', time, currentParams.velocity * 0.25);
        }, '2n');

        Tone.Transport.start();
        chordLoop.start(0);
        melodyLoop.start(0);
        bassLoop.start(0);
    }

    function stop() {
        if (melodyLoop) melodyLoop.stop();
        if (chordLoop) chordLoop.stop();
        if (bassLoop) bassLoop.stop();
        Tone.Transport.stop();
        isPlaying = false;
    }

    function noteToMidi(name, octave) {
        const map = {C:0,D:2,E:4,F:5,G:7,A:9,B:11};
        return (map[name] || 0) + (octave + 1) * 12;
    }

    return { init, updateParams, stop, ensureStarted };
})();
```

---

### 12.9 时间线 `static/js/timeline.js`

```javascript
const Timeline = (() => {
    let entries = [];
    let isReplaying = false;

    function addEntry(text, data) {
        entries.push({ timestamp: Date.now(), text, emotionData: data });
        const countEl = document.getElementById('entry-count');
        if (countEl) countEl.textContent = entries.length + ' 条';
        const bar = document.getElementById('timeline-progress');
        if (bar) bar.style.width = Math.min(100, entries.length / 20 * 100) + '%';
    }

    async function replay() {
        if (isReplaying || entries.length === 0) return;
        isReplaying = true;
        const btn = document.getElementById('playback-btn');
        btn.textContent = '⏹ 停止';

        for (let i = 0; i < entries.length; i++) {
            if (!isReplaying) break;
            const e = entries[i];
            displayText(e.text);
            MusicEngine.updateParams(e.emotionData.music);
            VisualEngine.updateParams(e.emotionData.visual);
            updateIndicators(e.emotionData);
            updateImageryTags(e.emotionData.imageries);
            const bar = document.getElementById('timeline-progress');
            bar.style.width = ((i+1)/entries.length*100)+'%';
            await sleep(3500);
        }
        isReplaying = false;
        btn.textContent = '▶ 回放';
    }

    function toggleReplay() {
        if (isReplaying) { isReplaying = false; }
        else { replay(); }
    }

    return { addEntry, toggleReplay };
})();
```

---

### 12.10 主控逻辑 `static/js/main.js`

```javascript
document.addEventListener('DOMContentLoaded', () => {
    VisualEngine.init();
    MusicEngine.init();
    Socket.connect();

    Socket.onResult(handleEmotionResult);

    const input = document.getElementById('text-input');
    const sendBtn = document.getElementById('send-btn');

    sendBtn.addEventListener('click', sendText);
    input.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') { e.preventDefault(); sendText(); }
    });

    document.getElementById('playback-btn')
            .addEventListener('click', () => Timeline.toggleReplay());

    // 点击任意位置启动Tone.js
    document.addEventListener('click', () => MusicEngine.ensureStarted(), { once: true });
});

let firstInput = true;

function sendText() {
    const input = document.getElementById('text-input');
    const text = input.value.trim();
    if (!text) return;

    // 首次输入：淡出标题，显示指示器和时间线
    if (firstInput) {
        firstInput = false;
        document.getElementById('title-area').classList.add('hidden');
        document.getElementById('emotion-indicator').classList.add('visible');
        document.getElementById('timeline-bar').classList.add('visible');
    }

    displayText(text);
    Socket.sendText(text);
    input.value = '';
    input.focus();
}

function handleEmotionResult(data) {
    MusicEngine.updateParams(data.music);
    VisualEngine.updateParams(data.visual);
    updateIndicators(data);
    updateImageryTags(data.imageries);
    Timeline.addEntry(data.text, data);
}

function displayText(text) {
    const display = document.getElementById('text-display');

    // 旧文字淡出
    const existing = display.querySelectorAll('.text-line');
    existing.forEach(el => {
        el.style.animation = 'textFadeOut 2s ease forwards';
    });

    // 保留最近3条
    while (display.children.length > 3) {
        display.removeChild(display.firstChild);
    }

    const line = document.createElement('div');
    line.className = 'text-line';
    line.textContent = text;
    display.appendChild(line);
}

function updateIndicators(data) {
    const vBar = document.getElementById('valence-bar');
    const vText = document.getElementById('valence-text');
    vBar.style.width = (data.valence * 100) + '%';
    vText.textContent = data.valence.toFixed(2);

    const aBar = document.getElementById('arousal-bar');
    const aText = document.getElementById('arousal-text');
    aBar.style.width = (data.arousal * 100) + '%';
    aText.textContent = data.arousal.toFixed(2);
}

function updateImageryTags(imageries) {
    const container = document.getElementById('imagery-tags');
    container.innerHTML = '';
    for (const word of imageries) {
        const tag = document.createElement('span');
        tag.className = 'tag';
        tag.textContent = word;
        container.appendChild(tag);
    }
}
```

---

## 十三、效果调试指南

### 13.1 音乐不好听

- 降低密度范围：`emotion_model.py` 中 `density = 0.15 + arousal * 0.45`
- 减小旋律跳跃：`music-engine.js` 中将 `(Math.random() - 0.5) * 4` 改为 `* 3`
- 增加休止概率：调高 `Math.random() > density` 的阈值

### 13.2 视觉效果不够酷

- 增加拖尾：`visual-engine.js` 中背景透明度从 `0.12` 降到 `0.06`
- 加大发光：`particle.js` 中增大 `glowSize` 值
- 增加粒子数：调高 `EFFECT_PRESETS` 中各效果的 `rate`

### 13.3 情绪识别不准

- 扩充词典：往 `imagery_dict.py` 添加更多词条
- 调整SnowNLP权重：在 `analyzer.py` 中调整 `valence + adjustment` 的系数
- 切换到API方案（`config.py` 中改为 `NLP_MODE = "api"`）

---

## 十四、扩展功能（加分项）

1. **语音输入**：使用 Web Speech API，`speechRecognition.onresult` 获取文字后发送
2. **导出视频**：`canvas.captureStream()` + `MediaRecorder` 录制mp4
3. **多人协作**：多客户端WebSocket同时连接，输入汇聚到同一画面
4. **MIDI导出**：用 `MidiWriter.js` 将旋律导出 `.mid` 文件
5. **诗词模式**：内置经典古诗，逐句自动输入播放
